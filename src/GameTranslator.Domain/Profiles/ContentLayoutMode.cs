namespace GameTranslator.Domain.Profiles;

/// <summary>
/// Selects the coordinated grouping, overlay and live-refresh policy for one saved OCR zone.
/// </summary>
public enum ContentLayoutMode
{
    DialogComic = 0,
}
