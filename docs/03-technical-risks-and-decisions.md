# Технические риски и спорные решения

## Проект

Система экранного OCR-перевода игровых субтитров и текста для Windows 11

Версия документа: 1.1 (обновлено)

------------------------------------------------------------------------

# 1. Назначение документа

Фиксация технических рисков, ограничений платформы, принятых архитектурных решений, которые запрещено менять без согласования.

------------------------------------------------------------------------

# 2. Общий принцип

Приоритет: стабильность, совместимость, минимальная нагрузка, простота поддержки. При конфликте красивой архитектуры и совместимости выбирается совместимость.

------------------------------------------------------------------------

# 3. Риск точности OCR игровых текстов

**Решение:** продукт сохраняет Windows OCR и Tesseract. По ADR-030 normal one-shot/live orchestration использует GPU Paddle detector, bounded grouping и Tesseract crop recognition; Paddle не заменяет recognizer. Нельзя удалять обязательный движок или делать продукт зависимым от одного OCR. Недоступность candidate runtime является контролируемым degraded-состоянием, а не основанием для скрытого fallback.

------------------------------------------------------------------------

# 4. Риск вертикального японского/китайского текста

**Решение:** Tesseract является обязательным recognizer для вертикального текста. Orientation поддерживает Auto/Horizontal/Vertical, а ручное переключение остаётся допустимым. Разрешённый ADR-030 Paddle detector предлагает только crop bounds внутри ручной зоны и не заменяет Windows OCR или Tesseract.

------------------------------------------------------------------------

# 5. Риск overlay поверх игр

**Решение:** основная цель – оконный режим и borderless fullscreen. Exclusive fullscreen не гарантируется. Запрещено использовать DLL Injection, Memory Reading, Memory Injection.

------------------------------------------------------------------------

# 6. Риск античитов

Программа работает как обычное приложение, не внедряется в процесс игры, не изменяет и не читает память игры.

------------------------------------------------------------------------

# 7. Риск высокой нагрузки на CPU

**Решение:** Frame Difference Detection, OCR только при изменении кадра, кэширование результатов. Пересмотренные целевые показатели: средний CPU ≤ 25%, пики до 50% на современном железе (6+ ядер).

------------------------------------------------------------------------

# 8. Риск перегрузки переводчиков

**Решение:** многоуровневый кэш (память + SQLite) с TTL 30 дней. Запрещено повторно переводить идентичный текст без проверки кэша.

------------------------------------------------------------------------

# 9. Риск неправильного позиционирования текста

**Решение:** raw OCR bounds сохраняются для маски и диагностики. Близкие raw blocks могут формировать semantic translation group с детерминированным reading order. Translation text bounds могут отличаться от mask bounds по действующему ADR-018: measured frame расширяется от source center до допустимых границ и только затем уменьшает шрифт. ADR-016 является историческим superseded-решением.

------------------------------------------------------------------------

# 10. Риск масштабирования разрешений

Хранение абсолютных и относительных координат. Приоритет относительных координат при изменении разрешения.

------------------------------------------------------------------------

# 11. Риск пересечения OCR-зон

Перекрытие запрещено. Проверка при сохранении профиля. Профиль не сохраняется при пересечении.

------------------------------------------------------------------------

# 12. Риск повреждения профилей

Обязательная валидация структуры, версии, типов данных, координат.

------------------------------------------------------------------------

# 13. Риск утечки API-ключей

Ключи хранятся только в Windows Credential Manager. Запрещено хранение в JSON, INI, XML, SQLite, логах.

------------------------------------------------------------------------

# 14. Спорное решение №1: выбор OCR

Почему не только Tesseract? Windows OCR остаётся поддерживаемым горизонтальным движком, а Tesseract необходим для вертикального текста и candidate crops. Принято по ADR-030: default orchestration использует GPU Paddle только как detector, затем Tesseract recognizer; Windows OCR и Tesseract не удаляются.

------------------------------------------------------------------------

# 15. Спорное решение №2: выбор языка

Почему не Python как приложение? WPF, Windows integration и orchestration остаются в C# + .NET. Принято по ADR-030: Python допустим только как закреплённый packaged worker GPU Paddle detector без UI, профилей, provider selection или overlay responsibility.

------------------------------------------------------------------------

# 16. Спорное решение №3: выбор базы данных

SQLite (не требует сервера, надежна, быстра). PostgreSQL/MySQL/MongoDB избыточны.

------------------------------------------------------------------------

# 17. Спорное решение №4: поддержка нескольких мониторов

Исключена из MVP (усложнение координат и overlay). Будущая версия.

------------------------------------------------------------------------

# 18. Спорное решение №5: автоматическое определение зон

Отказано из-за низкой надежности. Решение: ручная настройка сохранённых OCR-зон профиля.

ADR-030 не отменяет это решение. GPU candidate detector является штатным transient source-region provider внутри уже выбранной OCR-зоны; он не меняет профиль, не создаёт сохранённые зоны и не заменяет ручную настройку capture scope.

ADR-031 закрепляет `ContentLayoutMode` на уровне каждой сохранённой OCR-зоны. Это предотвращает смешивание будущих правил статичного меню или книги с правилами игрового диалога: grouping, overlay layout и minimum refresh interval изменяются согласованной mode policy, а не независимыми скрытыми эвристиками. Пока принят только behavior-preserving `DialogComic`; `Book`, `StaticMenu` и ненулевая cadence остаются отдельными продуктовым решениями.

------------------------------------------------------------------------

# 19. Уровни архитектурных решений

Hard-инварианты: отсутствие внедрения в память игры и DLL Injection, безопасное хранение секретов, направленные зависимости слоёв, поддержка существующих профилей через migration.

Decision Record: замена базовой платформы, persistence format, breaking contract, новая продуктовая семантика, KPI или vertical-text policy вне ADR.

Normal Delivery: позиционирование overlay, implementation tuning, cache/diagnostic bug fix, additive-compatible profile setting, тест и реализация внутри принятого ADR. Подробная классификация находится в `docs/governance/change-approval-required.md`.

------------------------------------------------------------------------

# 20. Критерий успеха проекта

- Перевод появляется менее чем за 500 мс после появления текста.
- Корректная замена оригинала.
- Приемлемая нагрузка на CPU (средняя ≤25%).
- Работа большинства современных игр в оконном и borderless fullscreen режимах.
- Обмен профилями через JSON без передачи API-ключей.
