# OCR and Overlay Work Status

Last updated: 2026-09-04

Purpose: durable chronology of overlay layout, OCR geometry/quality, candidate-pipeline, packaging, and owner evidence. For current state, read this file together with [Local unpublished worktree status](local-unpublished-worktree-status.md), GitHub Issues, and the working tree; dated track plans below are historical records unless the current guidance explicitly keeps an item open.

This file is a coordination note, not a new ADR and not a replacement for GitHub Issues. GitHub Issues and their explicit dependency graph remain the current delivery source. If an issue, accepted ADR, or explicit owner decision conflicts with this file, report the conflict and update this file after the owner decision.

## Current Baseline

- Product baseline before the current documentation sync: `main` commit `2ca05091c97944ec2726a045c6307d68beb8d6f7` (`Harden live translation and add guided setup`).
- Last completed source gates: Release build `0` warnings / `0` errors, full Release tests `645/645`, docs mini-check clean, and `git diff --check` clean.
- ADR-030 normal route is GPU Paddle detection → bounded writing-system grouping → Tesseract crop recognition → explicitly selected translator → per-region overlay. Detector/runtime failure is visible and does not select a hidden legacy/cache/provider fallback.
- ADR-018 source-centered measured overlay fit and ADR-031 per-zone `ContentLayoutMode.DialogComic` are active. ADR-016 is superseded; `Book` and `StaticMenu` are not implemented.
- CJK horizontal/vertical and Thai profile work is complete. Open language work is LTR owner-smoke (`#49`) plus Brahmic/Indic and RTL cohorts (`#53`-`#55`).
- The retained local r44 portable is source-equivalent, self-contained, unpacked, and locally verified. It has no transfer archive, signature, owner-live acceptance, or publication.
- Preserve the nine protected calibration/evidence paths listed in [Local unpublished worktree status](local-unpublished-worktree-status.md) until explicit owner disposition under `#35`.

## Status Legend

- `Not started`: no implementation in the current tracked plan.
- `Ready`: dependencies are satisfied enough to begin.
- `In progress`: implementation is active and not yet ready for owner verification.
- `In review`: implementation evidence is ready and awaits owner verification.
- `Blocked`: cannot proceed without an upstream task or owner decision.
- `Done`: implemented, verified, and status updated with evidence.

## Historical completed dependency graph (July-August 2026)

The graph below records the route that produced the accepted overlay/candidate baseline. It is not the active task queue; all named A/B tracks are complete or superseded by later ADR-030 work.

```text
Track A: Overlay
A1 WPF text measurement port
  -> A2 ADR-016 overlay placement
     -> A3 overlay evidence and issue update

Track B: OCR
B1 Tesseract word bounds and confidence
  -> B2 zone-mode PSM and grouping passes
     -> B3 Thai/CJK low-confidence fallback

Historical sequence only: A1/A2 and B1/B2/B3 were completed before the current candidate pipeline became the default.
```

## Track A: Overlay and Text Measurement

### A1. WPF text measurement port

Status: Done

Goal:
- Remove character-coefficient text measurement.
- Add an Application-level text measurement port or layout service contract.
- Implement WPF measurement in UI using the WPF text layout engine, preferably `TextFormatter` or equivalent WPF text formatting primitives that match runtime wrapping.

Implemented:
- Added the Application text measurement seam: `IOverlayTextMeasurer`, `OverlayTextMeasurementRequest`, `OverlayTextMeasurement`, and `OverlayTextLineMeasurement`.
- `OverlayPositioningService` now consumes the measurement seam for expanded overlay bounds and vertical font fitting.
- Added `WpfOverlayTextMeasurer` in UI using WPF `TextFormatter` line formatting, and registered it through presentation DI.
- Kept the parameterless `OverlayPositioningService` constructor backed by a legacy measurer only for existing offline/no-UI compatibility callers; production DI uses the WPF measurer.

Why first:
- ADR-016 placement needs real wrapped text dimensions. Implementing placement before real measurement would likely require rework.

Expected impact:
- Application contract addition and UI implementation.
- Existing overlay positioning code should consume measured line/box results instead of estimating character sizes.

Verification:
- Unit tests for deterministic wrapping/measurement decisions using a fake measurement service.
- UI/WPF-focused tests or smoke verification for the real WPF implementation when practical.
- Release build and relevant tests.
- 2026-07-29: focused tests passed, `32/32`: `OverlayPositioningServiceTests`, `OverlayPublicSeamTests`, and `PresentationCompositionTests`.
- 2026-07-29: `dotnet build GameTranslator.sln -c Release --no-restore` passed with `0` warnings and `0` errors.
- 2026-07-29: `dotnet test GameTranslator.sln -c Release --no-build` passed, `337/337`.

### A2. ADR-016/018 overlay placement

Status: Done

Goal:
- Use separate mask bounds and translation text bounds.
- Keep mask tied to OCR semantic/source bounds.
- Initialize vertical-source translation text width at about `2x` source width, clamped to readable min and OCR zone limits.
- Wrap text inside the measured box.
- Start from a minimally padded source-based frame and grow translation bounds symmetrically around the source center when actual measured content does not fit.
- Reduce font size only after available centered expansion is exhausted.
- Handle collisions against already placed translations, not only semantic OCR blocks.
- Surface deterministic clipping/ellipsis as a debug or quality warning when text still cannot fit.

Implemented:
- Vertical expanded text now uses a separate translation text box while the mask remains tied to the OCR semantic/source bounds.
- Vertical-source translation width starts at `2x` source width, with a readable minimum and OCR-zone/maximum-width clamps.
- Vertical expanded text grows around the source center within the OCR zone and only reduces font size after the zone height limit is reached.
- Placement collision handling now treats already placed translation text boxes as obstacles in addition to neighboring OCR semantic bounds.
- Clipped/overlapping placement fallbacks add deterministic `Overlay fit warning:` debug metric lines.

Owner-approved refinement completed:
- ADR-018 supersedes ADR-016's vertical-height-first policy. The common fit order is now source-based initial frame, measured centered expansion, then font reduction.
- Remove the horizontal right-overflow dampening heuristic that shifted real comic translations left of their source text.
- Add a session-only debug control for the vertical source-width multiplier (`1.0x` through `2.5x`, default `2.0x`); do not persist it in profiles or settings.

2026-07-29 implementation evidence:
- Replaced the old natural-width budget and semantic-right-overflow dampening with source-based initial frames. Horizontal text starts at source width plus `8px`; vertical text starts at `max(source + 8px, source x session multiplier)` with the existing readable-width floor.
- Measured content now expands width and height around the source center at the preferred font size. It reduces font size only after capture-region expansion cannot fit the text; at a capture boundary, the frame uses remaining valid space before reducing the font.
- Added `SessionVerticalSourceWidthMultiplier` to `OverlayPositioningService`, clamped to `1.0x` through `2.5x` and intentionally held only in memory. The WPF debug panel binds a disabled-until-debug slider/text input to it; no profile or `ISettingsService` write occurs.
- Focused placement and view-model tests passed, `91/91`.
- 2026-07-29: Release build passed with `0` warnings and `0` errors; full Release tests passed, `343/343`; `tools/check-docs-mini.ps1` reported `0` markdown link and `0` actionable backtick problems.

Verification:
- Focused placement tests for vertical CJK, long Russian translations, multi-zone independence, and collision handling.
- Evidence images must include legend, scenario numbers, and grouping mode (`menu`, `dialog`, or `comic`) when new evidence is generated.
- Avoid visible UI runs unless explicitly needed; report PID for any explicit UI run.
- 2026-07-29: `dotnet build GameTranslator.sln -c Release --no-restore` passed with `0` warnings and `0` errors.
- 2026-07-29: focused overlay placement tests passed, `24/24`: `OverlayPositioningServiceTests`.
- 2026-07-29: focused overlay/pipeline/composition tests passed, `57/57`: `OverlayPositioningServiceTests`, `OverlayPublicSeamTests`, `PresentationCompositionTests`, and `TranslationPipelineServiceTests`.
- 2026-07-29: `dotnet test GameTranslator.sln -c Release --no-build` passed, `340/340`.
- 2026-07-29: no visible UI run and no new evidence images generated in this step.

### A3. Overlay evidence and issue update

Status: Done

Goal:
- Produce no-UI/headless evidence where possible.
- Update relevant GitHub issue(s) with verified multi-line comments using `--body-file` or file-backed API JSON, then verify the published body.

Verification:
- `tools/check-docs-mini.ps1` if docs/evidence references are changed.
- Issue comment verification through `gh issue view --json comments` or `gh api`.
- 2026-07-29: generated local, untracked headless evidence through `OverlayPositioningService` with `WpfOverlayTextMeasurer` (`TextFormatter`); no UI window was opened. PNG: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\adr016-overlay-placement-2026-07-29\adr016-overlay-placement-evidence.png`; geometry report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\adr016-overlay-placement-2026-07-29\adr016-overlay-placement-evidence.json`.
- 2026-07-29: evidence scenarios are labeled `S1` through `S4` and carry `menu`, `dialog`, or `comic` grouping modes. `S1` proves separate vertical source/mask `36x178` and translation bounds `96x138`; `S2` proves wrapped dialog text without a warning; `S3` leaves a two-pixel gap between adjacent translation bounds; `S4` reaches the 8 px minimum font and emits the expected vertical clipping warning.
- 2026-07-29: focused overlay/public-seam/composition tests passed, `35/35`.
- 2026-07-29: generated additional local, untracked real-page evidence from a user-provided English comic page. PNG: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-comic-english-page-2026-07-29\real-comic-english-overlay-evidence.png`; OCR and geometry report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-comic-english-page-2026-07-29\real-comic-english-ocr-overlay-report.json`.
- 2026-07-29: this evidence used the production `TesseractOcrEngine` and `WpfOverlayTextMeasurer` headlessly. Current full-page single-block OCR emitted only five unusable lines; nine manually defined bubble crops produced non-empty OCR text in `8/9` cases, with illustration noise in several results. `B1` (`YO!`) returned no OCR text. Overlay placement had no fit warnings. Manual crop grouping is evidence-only and does not change B1/B2 status or claim automatic comic grouping.
- 2026-07-29: user-provided annotation established green semantic source/mask truth and blue per-bubble safe translation bounds for the same comic page. Local, untracked comparison PNG: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\annotated-comic-ground-truth-2026-07-29\annotated-comic-ground-truth-evidence.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\annotated-comic-ground-truth-2026-07-29\annotated-comic-ground-truth-report.json`.
- 2026-07-29: the annotation produced nine green and nine blue zones (the B8/B9 blue outlines touch, so generic connected-component counting sees eight components). Production Tesseract OCR returned non-empty text from `9/9` user-defined blue crops. With green bounds supplied as semantic source truth, current masks matched `9/9`; current translated text bounds met strict blue containment `0/9`, while emitting no fit warnings. This comparison exposed that horizontal width budgeting and right-overflow dampening are measured against semantic source bounds, not a translation layout frame.
- 2026-07-29 owner clarification: green is the semantic OCR/mask anchor; blue is evidence of the preferred, padded initial translation frame, not a persisted hard final constraint. Desired behavior is Google Translate-style: a translation box is initially minimal and centered around the original text, then expands only as actual WPF-measured content requires.
- 2026-07-29 owner approved the resulting default-policy change. ADR-018 supersedes ADR-016 and adds a session-only vertical width multiplier for debug evidence; A3 must be regenerated after A2 implementation before owner visual acceptance and a verified multi-line GitHub Issue `#32` comment.
- 2026-07-29 regenerated headless evidence with production `WpfOverlayTextMeasurer` and the owner-provided comic annotation. PNG: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\adr018-centered-comic-2026-07-29\adr018-centered-comic-evidence.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\adr018-centered-comic-2026-07-29\adr018-centered-comic-report.json`.
- 2026-07-29: new evidence uses green as semantic source/mask truth, blue as padded initial-frame calibration, and cyan as measured final translation bounds. Production Tesseract produced non-empty text from `9/9` supplied bubble crops; masks matched green `9/9`; final translation bounds remained centered after collision handling for `8/9`; warnings `0`. `B9` moved upward by `5.5px` to avoid the already placed `B8` translation bounds.
- 2026-07-30: generated two additional untracked headless evidence scenarios with production `TesseractOcrEngine` and `WpfOverlayTextMeasurer`; neither run opened a UI window. Both PNGs contain a legend, scenario identifier, and grouping mode. `S5` comic long-dialogue manga evidence: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-manga-expansion-2026-07-30\real-manga-expansion-2026-07-30-evidence.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-manga-expansion-2026-07-30\real-manga-expansion-2026-07-30-report.json`.
- 2026-07-30: `S5` uses six manually selected bubble crops for evidence only; Tesseract returned non-empty text for `6/6`, including the longest `M6` dialogue. The WPF overlay kept the preferred `18px` font for every translation and emitted `0` fit warnings. This is not an automatic comic-bubble detector.
- 2026-07-30: `S6` dialog real-game, two-line subtitle evidence: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-game-subtitle-2026-07-30\real-game-subtitle-2026-07-30-evidence.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-game-subtitle-2026-07-30\real-game-subtitle-2026-07-30-report.json`.
- 2026-07-30: `S6` returned text for `1/1` manually selected subtitle crop and created a `651x58` Russian translation bounds at `18px` with `0` overlay warnings. Tesseract included a decorative dialogue-frame glyph and grouped the source at `643x102`, so this is evidence that the pending word-bounds/confidence and zone-mode work (B1/B2, Issues `#38/#37`) remains necessary; it does not claim automatic scene-text detection.
- 2026-07-30: generated local-only, ignored `outputs/**` headless vertical CJK evidence with production `TesseractOcrEngine` and `WpfOverlayTextMeasurer`; no UI window was opened. `S7` comic Japanese PNG: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-japanese-vertical-2026-07-30\real-japanese-vertical-2026-07-30-evidence.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-japanese-vertical-2026-07-30\real-japanese-vertical-2026-07-30-report.json`.
- 2026-07-30: `S7` runs six manually selected dialogue crops using `jpn_vert` and vertical source geometry. Tesseract returned non-empty text for `6/6`; the overlay used the preferred `18px` font and emitted `0` fit warnings. Most narrow vertical columns produced narrow masks, while the large `J3` bubble was reported as an overly broad `180x277` source group and consequently received a `360px` translation frame. This is a current line-geometry/grouping limitation, not a claim of automatic comic grouping.
- 2026-07-30: generated local-only, ignored `outputs/**` headless vertical CJK evidence for `S8` comic Chinese. PNG: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-chinese-vertical-2026-07-30\real-chinese-vertical-2026-07-30-evidence.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-chinese-vertical-2026-07-30\real-chinese-vertical-2026-07-30-report.json`.
- 2026-07-30: `S8` runs six manually selected dialogue crops using `chi_sim_vert` and vertical source geometry. Tesseract returned non-empty text for `6/6` and the overlay emitted `0` fit warnings, but only the narrow `C3` crop (`54x283`) was recognized cleanly. The other crops include frame, illustration, or decorative-stroke noise in their source geometry; their wide masks correctly cause wide translation frames under the current ADR-018 policy. Tesseract also reported non-fatal small-image/line-recognition diagnostics. This evidence keeps `B1` word bounds/confidence and `B2` zone-mode grouping (`#38` then `#37`) as the next OCR-quality work.
- 2026-07-30 owner clarification from annotated Japanese and Chinese vertical pages: narrow vertical source groups must exhaust symmetric horizontal translation-frame growth before the layout increases its height; font reduction remains last. Horizontal-source behavior remains unchanged. Added the focused regression test `CreateSnapshot_WithExpandedVerticalText_ExhaustsWidthBeforeIncreasingHeightAllowance`; `OverlayPositioningServiceTests` passed `27/27`.
- 2026-07-30: owner annotations use exact green semantic source/mask bounds and blue padded initial-frame calibration. Generated local-only, ignored `outputs/**` owner-geometry evidence with production `WpfOverlayTextMeasurer` and no UI window. `S9` comic Japanese PNG: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\owner-annotated-japanese-vertical-2026-07-30\owner-annotated-japanese-vertical-2026-07-30-evidence.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\owner-annotated-japanese-vertical-2026-07-30\owner-annotated-japanese-vertical-2026-07-30-report.json`.
- 2026-07-30: `S9` uses owner-confirmed source geometry for ten vertical Japanese groups. `S10` comic Chinese PNG: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\owner-annotated-chinese-vertical-2026-07-30\owner-annotated-chinese-vertical-2026-07-30-evidence.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\owner-annotated-chinese-vertical-2026-07-30\owner-annotated-chinese-vertical-2026-07-30-report.json`.
- 2026-07-30: `S10` uses owner-confirmed source geometry for six vertical Chinese groups. Both owner-geometry runs intentionally bypass OCR so they isolate placement: every mask equals the supplied green bounds, blue remains calibration only, final cyan bounds are measured through `TextFormatter`, and both runs emitted `0` overlay warnings. They do not change the earlier conclusion that production CJK OCR geometry still needs B1/B2.
- 2026-07-30: owner visually accepted the `S9` Japanese and `S10` Chinese owner-geometry evidence. Published and verified the multi-line Issue `#32` checkpoint comment: `https://github.com/Dementius-cell/Game-Translator/issues/32#issuecomment-5127697636`. `A3` is complete; Issue `#32` remained open only for the raw OCR geometry dependency chain `#38` then `#37`.
- 2026-08-01: after `#38` and `#37` were completed and closed, published and verified the final dependency-sync comment for Issue `#32`: `https://github.com/Dementius-cell/Game-Translator/issues/32#issuecomment-5150385318`. Issue `#32` is closed as completed for the approved vertical/CJK overlay placement hardening scope.

## Track B: OCR Geometry and Quality

### B1. Tesseract word bounds and confidence

Status: Done

Decision record:
- 2026-07-30: the owner approved additive optional word metadata to reach the source-geometry fidelity shown by the accepted CJK evidence. ADR-019 is accepted; the verified Issue #38 decision comment is `https://github.com/Dementius-cell/Game-Translator/issues/38#issuecomment-5127956743`.
- B1 is limited to propagating actual word text, bounds, nullable confidence, and recognition-pass identity without changing existing line-block behavior. Automatic rejection, retries, preprocessing fallback, profile settings, and persistence remain B2/B3 work.

Completion:
- 2026-07-30: added engine-neutral `OcrResult.Words`. Every `OcrWord` carries text, frame-relative bounds, nullable engine-local confidence, and a recognition-pass identifier. Existing line blocks remain unchanged; engines that do not produce word metadata return an empty list.
- 2026-07-30: `TesseractOcrEngine` now maps `textLine.Words` from the `TesseractOCR` layout API. It keeps Tesseract's actual word bounds and confidence and records the selected pass as `tesseract:SingleBlock` or `tesseract:SingleBlockVertText`.
- 2026-07-30: raw word metadata is preserved by frame-reuse and current semantic text grouping so B2 can consume it; B1 does not change grouping, rejection, preprocessing, or fallback policy.
- 2026-07-30: local-only headless production evidence confirmed metadata on the owner-provided real CJK pages without opening a UI window. `S7` comic Japanese produced 75 full-page words and crop counts `J1=7, J2=6, J3=23, J4=18, J5=12, J6=10`: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-japanese-word-metadata-2026-07-30\real-japanese-vertical-2026-07-30-report.json`. `S8` comic Chinese produced 140 full-page words and crop counts `C1=4, C2=13, C3=4, C4=10, C5=3, C6=2`: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-chinese-word-metadata-2026-07-30\real-chinese-vertical-2026-07-30-report.json`.
- 2026-07-30: the evidence intentionally confirms raw engine output, including low/zero-confidence noise near illustration and frame strokes. It does not claim that the current single-block OCR already reproduces the accepted green semantic bounds; that remains B2 zone-mode detection and grouping.
- 2026-07-30 verification: focused OCR/grouping/architecture tests `83/83`; `dotnet build GameTranslator.sln -c Release --no-restore` with `0` warnings and `0` errors; full `dotnet test GameTranslator.sln -c Release --no-build` `347/347`; `tools/check-docs-mini.ps1` `0` markdown-link and `0` actionable-backtick problems.

Goal:
- Extend the Tesseract OCR path to expose word-level bounds and confidence.
- Preserve existing line/block output compatibility or provide an additive representation that downstream code can group.
- Enable filtering or quality warnings for low-confidence recognitions instead of blindly accepting unreadable output.

Why first:
- Confidence is needed before expensive fallback modes can be applied selectively.
- Word geometry is needed for honest multi-pass grouping and better overlay source geometry.

Expected impact:
- Tesseract adapter and Application OCR result contracts may need additive fields.
- Tests should prove existing consumers still work and new word/confidence data is available.

Verification:
- Focused Tesseract tests with fixture images.
- Architecture tests to ensure contracts stay in the right layer.
- Release build and relevant tests.

### B2. Zone-mode PSM and grouping passes

Status: Done

Decision record:
- 2026-07-30: B1 / Issue #38 completed and closed. The owner approved the resulting layout-aware default policy in ADR-020; the verified Issue #37 decision comment is `https://github.com/Dementius-cell/Game-Translator/issues/37#issuecomment-5128079069`.
- Existing `TranslationGroupingMode` is the compatible runtime layout signal: `BlockByBlock` -> menu/sparse, `WholeZone` -> dialog/single block, and `NearbyBlocks` -> comic/sparse detection plus line refinement. No profile schema or persisted layout setting is added.

Completion:
- 2026-07-30: added the additive runtime `OcrLayoutMode` contract. Pipeline derives it from the existing grouping setting; preprocessing and OCR request reconstruction preserve it.
- 2026-07-30: Tesseract maps menu to `SparseText` (`PSM 11`), dialog to the orientation-aware single-block pass, and comic to sparse detection followed by per-detected-line refinement. The wrapper page is disposed before each refinement because it permits only one active `Process` result per engine.
- 2026-07-30: comic semantic source blocks now use refinement word bounds with Tesseract confidence at least `50`, while all detection and refinement words remain available as pass-labelled raw diagnostics. Low-confidence words cannot expand a source mask to illustration or frame strokes.
- 2026-07-30: direct no-UI evidence exercised dialog and comic layout selection. `S6` dialog real-game subtitle returned text `1/1` with `0` overlay warnings: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-game-dialog-layout-2026-07-30\real-game-subtitle-2026-07-30-evidence.png`. `S7` comic Japanese returned text `6/6`; word geometry produces compact masks and short vertical translation no longer grows to capture width: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-japanese-comic-short-vertical-fit-2026-07-30\real-japanese-vertical-2026-07-30-evidence.png`.
- 2026-07-30: `S8` comic Chinese produced only `3/6` reliable groups. This is recorded as B3 input, not accepted CJK quality: low-confidence/noisy candidates are suppressed from semantic geometry instead of overlaying a broad translation onto artwork.

Goal:
- Choose Tesseract page segmentation mode by zone grouping/layout mode.
- Menu: use sparse text style segmentation, expected Tesseract `PSM 11`.
- Dialog: use uniform block segmentation, expected Tesseract `PSM 6`.
- Comic: run sparse detection first, then OCR individual lines/groups.
- Replace the current single `SingleBlock` / `SingleBlockVertText` choice for all layouts.

Why after B1:
- Multi-pass OCR should use word bounds/confidence to decide grouping quality and avoid producing misleading geometry.

Verification:
- Tests for menu, dialog, and comic grouping behavior.
- Evidence images with scenario number and grouping mode.
- Performance notes when additional passes are introduced.

### B3. Thai/CJK low-confidence fallback

Status: Done

Decision record:
- 2026-07-30: the owner-approved empty-comic fallback is recorded in ADR-021. It is bounded to a single orientation-aware Tesseract single-block pass only after comic sparse/refinement emits zero reliable semantic groups; no profile field, unconditional retry, or preprocessing default is introduced.
- 2026-07-30: ADR-022 is accepted for the next bounded B3 increment. It adds one empty-only `2x` bilinear quality-upscale retry for Tesseract CJK/Thai languages, with original-frame bounds mapping and explicit diagnostic pass identifiers. It does not add persisted profile settings, inversion, adaptive Otsu/Sauvola, deskew, or `tessdata_best`.

Progress:
- 2026-07-30: implemented pass-labelled `empty-comic-fallback` with the same `>= 50` confidence geometry rule as B2. It preserves raw fallback words for diagnostics and cannot restore broad line-rectangle masks.
- 2026-07-30: headless `S8` comic Chinese evidence improved from `3/6` to `5/6` non-empty crop results: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-chinese-comic-empty-fallback-2026-07-30\real-chinese-vertical-2026-07-30-evidence.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\real-chinese-comic-empty-fallback-2026-07-30\real-chinese-vertical-2026-07-30-report.json`.
- 2026-07-30: the report confirms `C3` recovered through `tesseract:SingleBlockVertText:empty-comic-fallback` with a `68x197` source bounds. `C6` still has no reliable words and C1/C4/C5 have incomplete or overly broad geometry. These remain the required preprocessing/model-quality evidence before B3 can be complete.
- 2026-07-30: local-only evidence-only preprocessing sweep compared baseline, existing production threshold/scale, bilinear `2x`/`3x`, Otsu, Sauvola, and inversion variants. Chinese report/contact sheet: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\b3-preprocessing-sweep-noscale-2026-07-30\real-chinese-b3-preprocessing-sweep-2026-07-30\b3-preprocessing-sweep-report.json`; Thai reports are under `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\b3-preprocessing-sweep-2026-07-30\thai-b3-preprocessing-sweep-*`.
- 2026-07-30: evidence showed Thai baseline already reliable on the available calibration crops, while Chinese `C6` only became useful when `2x` upscale happened before comic sparse detection and line refinement. No-scale threshold/Otsu/Sauvola mostly produced single-symbol noise for `C6`; `tessdata_best`, deskew, persisted profile settings, and unconditional retries were not promoted to production.
- 2026-07-30: implemented ADR-022 in `TesseractOcrEngine`: empty CJK/Thai results run one `2x` bilinear quality-upscale retry. Comic layout retries sparse detection plus line refinement and maps diagnostics/semantic bounds back to the source frame; non-comic layout retries the selected PSM and maps bounds back.
- 2026-07-30: production headless `S8-B3` comic Chinese sweep now reports baseline non-empty semantic output for `6/6` crops, `11` semantic blocks, `28` reliable words, average reliable confidence `78.21`, total baseline time `1081.97ms`. `C6` recovered through `tesseract:SparseText:quality-upscale-detection` and `tesseract:SingleBlockVertText:quality-upscale-line-refinement` with `2` semantic blocks, `4` reliable words, and average reliable confidence `84.07`: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\b3-production-comic-quality-upscale-2026-07-30\real-chinese-b3-preprocessing-sweep-2026-07-30\b3-preprocessing-sweep-contact-sheet.png`; report: `C:\Users\admin\Documents\Codex\2026-07-29\game-translator-continuation-2026-07-29\outputs\b3-production-comic-quality-upscale-2026-07-30\real-chinese-b3-preprocessing-sweep-2026-07-30\b3-preprocessing-sweep-report.json`.
- 2026-08-01: the owner accepted the B3 production evidence. ADR-022 completes the bounded empty-result fallback; partial non-empty OCR, including still-imperfect C1/C4/C5 Chinese geometry/text, deliberately remains outside this scope. Any future fallback for wrong-but-non-empty output needs a separate confidence/geometry quality policy and evidence.
- 2026-07-30 verification: `dotnet build GameTranslator.sln -c Release --no-restore` passed with `0` warnings and `0` errors; full `dotnet test GameTranslator.sln -c Release --no-build` passed `361/361`; `tools/check-docs-mini.ps1` reported `0` markdown-link and `0` actionable-backtick problems.
- 2026-08-01: published and verified the final Issue `#37` B3 sync comment: `https://github.com/Dementius-cell/Game-Translator/issues/37#issuecomment-5150383025`. Issue `#37` is closed as completed under ADR-020/021/022 scope.

Goal:
- Add bounded fallback only when confidence or geometry indicates poor OCR, without profile or persistence changes unless evidence justifies them.
- Candidate preprocessing: inversion for light text on dark background, adaptive Otsu/Sauvola-style binarization, deskew, and higher-quality upscale.
- Candidate OCR fallback: `tessdata_best` only for low-confidence cases because it is slower than fast data.
- Thai uses Tesseract `tha` on this Windows build; Windows OCR Thai is unavailable in the known environment.

Why last:
- Without confidence, expensive fallback would run blindly.
- Without zone-mode PSM, fallback may improve pixels while layout segmentation remains wrong.

Verification:
- Thai/CJK fixture tests and evidence.
- Latency/performance measurement for fallback paths.
- Confirm fallback does not become an unconditional default.

## Track C: Modern Scene-Text OCR Research

### C1. Issue #39 PaddleOCR local benchmark

Status: Done

Scope:
- Research-only benchmark for an optional modern scene-text OCR engine candidate.
- No product code, runtime dependency, OCR default, profile schema, release packaging, model files, cloud OCR/VLM, game hooks, memory reads, or input interception are introduced.
- Any future integration still requires an owner-approved Change Request/ADR because it would add a third OCR runtime and deployment footprint.

Evidence:
- 2026-08-01: created a local-only ignored benchmark workspace at `C:\Users\admin\Documents\Codex\2026-06-10\github-game-translator\outputs\issue-39-scene-ocr-benchmark-2026-08-01`.
- Local environment: Python `3.12.13`, PaddlePaddle `3.3.0`, PaddleOCR `3.7.0`, Windows 11 CPU. The benchmark venv is `811.53 MiB`; downloaded model cache is `231.21 MiB`.
- Official PaddleOCR `PP-OCRv6` supports `en`, `ch`, and `japan` in this run. Thai is not available through `PP-OCRv6`, so the Thai case uses `lang=th` with `PP-OCRv5`.
- The default Windows CPU oneDNN/MKLDNN path failed before OCR output with Paddle runtime `NotImplementedError`; all successful benchmark cases required `enable_mkldnn=False`.
- JSON report: `C:\Users\admin\Documents\Codex\2026-06-10\github-game-translator\outputs\issue-39-scene-ocr-benchmark-2026-08-01\results\paddleocr-benchmark-report.json`.
- Markdown report: `C:\Users\admin\Documents\Codex\2026-06-10\github-game-translator\outputs\issue-39-scene-ocr-benchmark-2026-08-01\results\paddleocr-benchmark-report.md`.
- Evidence PNGs include a legend, scenario id, and grouping mode:
  - `P1` dialog game subtitle full frame: `19.63s` first run, `19.47s` steady, `10` regions, expected subtitle substrings `3/3`, but it also detects unrelated HUD text.
  - `P2` comic English manga full page: `21.13s` first run, `21.43s` steady, `38` regions, most dialogue text detected automatically; reading order interleaves left/right bubbles and still needs grouping before overlay use.
  - `P3`/`P4` comic Japanese vertical full page: `12.20s` to `13.45s`, `20` regions. It detects many vertical dialogue columns on the full page; textline orientation did not materially improve this case.
  - `P5`/`P6` comic Chinese vertical full page: about `20s`, `11` regions. It detects several vertical blocks but misses/merges some dialogue and includes punctuation/page noise; textline orientation did not materially improve this case.
  - `P7` dialog Thai crop: first run included `130.73s` model download/init; cached OCR is about `4.06s` steady for `3` Thai regions. Current accepted Tesseract Thai evidence on the same crop is tens of milliseconds with reliable confidence.

Recommendation:
- Defer product integration for now. PaddleOCR is promising as a research/reference detector, especially for full-page comic text discovery, but the current Windows CPU latency, venv/model footprint, MKLDNN workaround, and unresolved grouping/reading-order work make it unsuitable as a normal runtime dependency without a separate owner-approved ADR.
- Keep Windows OCR plus Tesseract as the product OCR engines. Revisit only if the owner wants a new ADR focused on optional packaged model deployment, per-zone async/background use, and a concrete quality win over the current B1/B2/B3 pipeline on real user captures.

Verification:
- Local-only ignored benchmark script: `C:\Users\admin\Documents\Codex\2026-06-10\github-game-translator\outputs\issue-39-scene-ocr-benchmark-2026-08-01\run_paddleocr_benchmark.py`.
- No UI window was opened.
- No product source or test code was changed for this research checkpoint.
- 2026-08-01: `tools/check-docs-mini.ps1` reported `0` markdown-link and `0` actionable-backtick problems.
- 2026-08-01: published and verified the final Issue `#39` research summary comment: `https://github.com/Dementius-cell/Game-Translator/issues/39#issuecomment-5150366070`. The owner approved closing `#39`; Issue `#39` is closed as completed with product integration deferred.

## Track D: Full-Page Comic OCR Geometry Hardening

Status: Done; the failed full-page baseline led to the accepted ADR-030 candidate-region default. The plan below is historical.

Owner-approved delivery plan:
1. Add a full-page comic geometry golden gate using the accepted `S9` Japanese and `S10` Chinese owner annotations as reference geometry. The gate must score full-page OCR output against expected semantic source bounds, extra detections, missed groups, reading order, and downstream overlay warnings/overlaps.
2. Add a quality policy for wrong-but-non-empty OCR output. Empty-only fallback from B3 is complete, but production still needs a bounded way to reject or retry OCR that returns text with bad geometry, low reliable-word support, overly broad source bounds, or illustration/frame noise.
3. Strengthen comic grouping on top of B1 word metadata. The target pipeline is full page -> OCR words -> reliable-word filtering -> vertical/horizontal clusters -> semantic source bounds -> reading order.
4. Split broad or noisy comic groups instead of accepting a wide line rectangle as the source mask. Known pressure cases include the accepted Chinese page regions similar to `C1`, `C4`, and `C5`.
5. Add bounded preprocessing fallback for poor geometry, not only empty results. Candidate retries include the already accepted `2x` quality upscale and possible threshold/inversion/deskew variants, but they must be gated by evidence and owner-approved ADR scope before changing runtime behavior.
6. Under ADR-023, evaluate a GPU candidate-detector strategy on the RTX 3060 8GB benchmark baseline after the Tesseract quality gate is measurable. The detector may propose transient regions only; any third OCR runtime still requires a separate owner-approved integration ADR.

Implementation notes:
- 2026-08-01: current Release overlay placement was checked headlessly on the original owner-provided `S9`/`S10` manga pages with owner semantic geometry. The report is local-only: `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\release-headless-manga-overlay-check-2026-08-01\release-headless-manga-overlay-check-report.json`. Results: `S9` `10/10` groups, `0` warnings/overlaps/out-of-page/mask mismatches; `S10` `6/6` groups, `0` warnings/overlaps/out-of-page/mask mismatches. This confirms placement, not automatic full-page OCR detection.
- First implementation increment: add a reusable reference-based geometry quality gate around `OcrResult.TextBlockSources.SemanticBounds`. This does not change OCR runtime behavior or add a third OCR engine; it creates the measurable fail/pass surface needed before wrong-but-non-empty fallback work.
- 2026-08-01: added `ComicGeometryQualityGate` in `src/GameTranslator.Application/Ocr/ComicGeometryQualityGate.cs` with pass/fail metrics for reference matches, misses, extra detections, and reading-order violations. Added focused owner-reference tests in `tests/GameTranslator.Tests/Application/ComicGeometryQualityGateTests.cs` covering accepted `S9`/`S10` bounds, broad wrong-but-non-empty geometry, interleaved reading order, and extra noise.
- 2026-08-01 verification: focused `ComicGeometryQualityGateTests` passed `5/5`; Release build passed with `0` warnings and `0` errors; focused gate plus OCR public seam tests passed `8/8`; `tools/check-docs-mini.ps1` reported `0` markdown-link and `0` actionable-backtick problems.
- 2026-08-01: connected the gate to a local-only headless full-page production OCR baseline runner at `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\work\full-page-comic-geometry-baseline-2026-08-01`. It uses repository-local `tessdata`, `TesseractOcrEngine`, `OcrOrientationMode.Vertical`, and `OcrLayoutMode.Comic` without opening UI.
- 2026-08-01 full-page baseline report: `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\full-page-comic-geometry-baseline-report.json`; summary: `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\full-page-comic-geometry-baseline-summary.md`; evidence PNGs: `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\s9-full-page-production-ocr-geometry-evidence.png` and `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\s10-full-page-production-ocr-geometry-evidence.png`.
- 2026-08-01 baseline result: `S9` failed the gate with `0/10` reference groups matched, `10` missed, `24` extra detections, `149` words, `59` reliable words, and about `795ms` OCR time. `S10` failed the gate with `0/6` matched, `6` missed, `10` extra detections, `111` words, `24` reliable words, and about `1140ms` OCR time. This establishes the current production full-page comic OCR baseline as wrong-but-non-empty: text fragments are detected, but semantic source geometry is too fragmented or noisy to drive owner-like overlay masks.
- 2026-08-01 repeat headless Release full-page run: production raw geometry again failed at `S9 0/10` and `S10 0/6`. Local-only research grouping produced only `S9 2/10` with one extra and `S10 0/6`; it remains unwired. The bounded `2x` research pass reached pre-grouping candidate recall `S9 7/10` and `S10 3/6`, while outside reliable candidates increased to `26` and `45`, respectively. Summary and copied JSON/PNG evidence are local-only at `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-track-d-adr-023\outputs\full-page-ocr-release-run-2026-08-01\summary.md`; no UI window was opened and no production OCR/retry policy changed.
- 2026-08-01 second implementation increment: added `ComicTextGeometryGroupingService` in `src/GameTranslator.Application/Ocr/ComicTextGeometryGroupingService.cs`. It receives confident words from one OCR recognition pass, filters low-confidence and wide horizontal candidates, groups adjacent vertical CJK fragments while retaining every member bound, emits right-to-left/top-to-bottom text order inside a group, and suppresses single-fragment geometry. Focused tests in `tests/GameTranslator.Tests/Application/ComicTextGeometryGroupingServiceTests.cs` cover adjacent-column grouping, separated vertical groups, low-confidence/wide-noise rejection, and singleton suppression. The service is deliberately not wired into `TesseractOcrEngine` yet: production pass selection, real-page score improvement, and a wrong-but-non-empty fallback policy remain separate evidence/approval work.
- 2026-08-01 local-only follow-up applied the service only to `SingleBlockVertText:line-refinement` words from the same full-page baseline harness. `S9` supplied `73` refinement words and produced `3` compact groups, but still matched `0/10` owner bounds with `3` extras. `S10` supplied `50` refinement words and produced `0` eligible groups, so it still matched `0/6`. The regenerated local-only report and summary are at `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\full-page-comic-geometry-baseline-report.json` and `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\full-page-comic-geometry-baseline-summary.md`; grouped evidence PNGs are `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\s9-refinement-word-grouping-geometry-evidence.png` and `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\s10-refinement-word-grouping-geometry-evidence.png`. The result is a fail-safe rejection rather than a geometry improvement. Next code work must improve candidate fragment recovery/selection before considering runtime wiring; weakening the current filters would reintroduce broad or noisy overlay masks.
- 2026-08-01 third implementation increment: added the engine-neutral `ComicGeometryCandidateRecallAnalyzer` in `src/GameTranslator.Application/Ocr/ComicGeometryCandidateRecallAnalyzer.cs` and focused tests in `tests/GameTranslator.Tests/Application/ComicGeometryCandidateRecallAnalyzerTests.cs`. Before grouping, a reference is supported only when at least two words with confidence `>= 50` are each at least `50%` inside its owner semantic bounds; reported coverage is the non-duplicated union of those word rectangles. This measures candidate recall separately from semantic-group geometry.
- 2026-08-01 local-only candidate-recall comparison used the existing full-page harness and original `S9`/`S10` pages. At `1x`, all production words supported `6/10` S9 references and `1/6` S10 references. The already accepted `2x` quality-upscale experiment improved that to `7/10` and `3/6`, respectively, but increased outside reliable candidates from `11` to `26` on S9 and from `20` to `45` on S10; OCR time rose from about `780ms` to `1262ms` on S9 and from `1095ms` to `3454ms` on S10. Per-reference inspection shows no unused complementary recovery sufficient to close the missing S9/S10 bounds. The full local-only metrics are in `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\full-page-comic-geometry-baseline-report.json` and `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-next-stage-2026-08-01\outputs\full-page-comic-geometry-baseline-2026-08-01\full-page-comic-geometry-baseline-summary.md`.
- 2026-08-01 decision boundary: current full-page Tesseract candidates remain insufficient for reliable production geometry, even with the bounded `2x` experiment. Do not wire the grouping service or add a wrong-but-non-empty runtime retry from this evidence, and do not start an unbounded Tesseract parameter sweep. A future production direction needs an owner decision on a separately bounded candidate-recovery policy or a detector-strategy ADR; full-page crop/zone OCR remains the proven path for the current engine.
- 2026-08-01 owner decision: adopt ADR-023 progressive per-region overlay delivery. After a region stabilizes, its OCR, translation, and overlay may complete independently; a complex region must not block a simple region from appearing before it leaves a 3–4 second screen interval. Cancellation is region-scoped, and saved OCR zones remain manual capture scope. The benchmark floor for GPU candidate-detector research is an NVIDIA RTX 3060 with 8 GiB VRAM and a CPU with at least six cores. ADR-023 authorizes benchmark/design work only, not a PaddleOCR or other third-runtime production integration.
- 2026-08-01 GitHub governance sync: created owner-approved follow-up `#41` (`https://github.com/Dementius-cell/Game-Translator/issues/41`); published and verified the ADR-023 owner summary in `#28` (`https://github.com/Dementius-cell/Game-Translator/issues/28#issuecomment-5151084543`); closed the original MVP PRD `#1` as superseded historical planning after publishing its traceability comment (`https://github.com/Dementius-cell/Game-Translator/issues/1#issuecomment-5151084856`).

### D2. Approved Progressive Delivery Increment

Status: Done; Issue `#41` was closed on 2026-08-15.

- `TranslationPipelineService.RunAllZonesAsync` starts the existing saved OCR zones independently and publishes a combined overlay whenever one zone completes. A ready simple zone therefore does not wait for a slow zone from the same batch.
- The in-memory translation cache is synchronized for concurrent zone work. No OCR engine, profile field, persisted zone, retry policy, or UI workflow was added or replaced.
- The deterministic pipeline test proves that a completed simple zone is shown while another zone is still blocked in translation. Focused pipeline/cache/headless-overlay/live-loop verification passed `37/37` on 2026-08-01; the full Release suite passed `387/387`.
- The headless S9/S10 overlay integration test checks owner geometry for no overlaps, out-of-page placement, or source-mask regression. It validates placement from accepted source bounds; it does not claim automatic full-page text discovery.
- The live loop now owns `TranslationPipelineService.LiveTranslationSession`. Each tick compares the complete captured source identity for every saved zone. New pixels cancel only the active work for that zone; unchanged work keeps running, and its completed overlay remains combined with other completed zones.
- Deterministic fakes cover a changed slow region, a disappeared slow region, and two pending regions where only the changed one is cancelled. Existing live start/stop smoke coverage confirms that global stop still hides every overlay.
- 2026-08-01: a local-only headless Release runner replayed preserved real S9/S10 page pixels through the live session with deterministic fake OCR/translation, so it isolated orchestration rather than OCR quality. It proved ready-before-slow publication, changed-region-only cancellation, disappeared-region-only removal, and global-stop cancellation plus final overlay removal; the report is `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-track-d-adr-023\outputs\headless-release-live-session-2026-08-01\headless-release-live-session-report.json` (local-only output). The run did not open a UI window or change OCR runtimes, profile fields, or retry policy.

### D3. GPU Candidate-Detector Research Decision

Status: Done as historical research; the gated pilot progressed through owner evidence to ADR-030 default adoption.

- 2026-08-02 local-only follow-up: the GPU PaddleOCR higher-resolution-balanced detector with research-only vertical merge-gap factor `0.5` was stable across `30` repeated runs on each owner page. It produced `S9 10/10` and `S10 6/6` owner geometry matches; before filtering it still had `1` and `5` outside candidates. Validated detector report and red/green candidate evidence: `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-track-d-adr-023\outputs\issue-41-vertical-merge-gap-probe-2026-08-02\candidate-detector-benchmark-report.json`.
- 2026-08-02 local-only Tesseract crop follow-up: applying existing Tesseract `Dialog` recognition to each transient candidate returned non-empty text for all `11/11` S9 and `10/11` S10 candidates; every owner-matched S9/S10 candidate returned text. A research-only post-filter (candidate height at least `4%` of the saved-zone height, height/width at least `1.0`, and at least two Japanese/Chinese script characters) retained exactly `S9 10/10` and `S10 6/6` owner groups with `0` extras. This is source-geometry/text-presence evidence on two pages, not character-accuracy evidence or a production rule. Report: `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-track-d-adr-023\outputs\issue-41-vertical-merge-gap-probe-2026-08-02\candidate-crop-tesseract-research-summary.md`.
- GPU Paddle detection had strong S9 recall but previously missed S10 owner geometry and produced outside candidates. The new research result corrects the observed over-merge, but remains a two-page, detector-plus-Tesseract experiment without an approved runtime policy.
- GPU EasyOCR CRAFT detection found `9/10` S9 owner groups with `5` outside candidates and `4/6` S10 groups with `6` outside candidates. Steady detector timing met the 3.5-second visibility scenario, but the predeclared quality rule required all `16/16` owner groups and at most one outside candidate per page.
- 2026-08-02 expanded local-only research: the transitive merge failure was reproduced on public vertical Chinese documents. A deterministic `cap-4 + dense-top-alignment` guard preserved S9 `10/10`, S10 `6/6`, and the initial Japanese corpus `34/34`, while restoring Chinese tight-line geometry from `67/198` to `194/198`. On an additional 25-page OpenMantra Japanese holdout, guarded candidates mapped `200/206` speech regions; existing Tesseract returned non-empty text for `198/200` mapped crops with micro CER `11.7%` (`238/2040`). On an additional 25-page vertical Chinese document geometry stress-test, baseline transitive merge matched `110/1182` lines and the guard matched `1179/1182`. Reports are generated/local-only under `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-track-d-adr-023\outputs\issue-41-capped-merge-research-2026-08-02` and `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-track-d-adr-023\outputs\issue-41-expanded-external-corpus-2026-08-02`; the document corpus is a geometry stress-test, not a manhua OCR claim.
- 2026-08-02 governance: #28 is closed and #41 is unblocked. Change Request [#42](https://github.com/Dementius-cell/Game-Translator/issues/42) records the pilot decision. ADR-024 originally accepted the bounded opt-in detector pilot; owner-approved ADR-025 supersedes only its cold initialization-plus-first-result P95 from `<= 2.0 s` to `<= 5.0 s`. All quality, hardware, packaging, compatibility, and disabled-by-default boundaries remain in force.
- 2026-08-02 ADR-024 implementation increment: added the engine-neutral `ITextCandidateDetector` port and `TextCandidateRegionOcrService`. It accepts only bounded, non-overlapping, in-zone transient candidates, crops the already captured zone frame, and forces every accepted crop through existing Tesseract recognition. `TranslationPipelineRunOptions.EnableCandidateDetectorPilot` is session-only and defaults to `false`; when enabled without an available detector, it emits no detector-derived overlay and does not fall back to legacy full-page OCR. No profile, UI, persisted setting, `IOcrEngine`, or retry policy changed.
- 2026-08-02 ADR-024 runtime increment: added a persistent local `PaddleOcrTextCandidateDetector` worker adapter and the PaddleOCR text-detection script. The adapter is registered behind the Application port but requires the Python runtime packaged in the release candidate-detector folder; if it, the worker, the GPU, or the worker response is unavailable, it returns no candidates. The current product output has no packaged Python runtime, so the pilot remains unavailable by design. Runtime/model packaging, license notice, checksum, offline install, and rollback evidence remain release gates.
- 2026-08-02 hardware/package preflight: the machine exceeds the ADR-024 floor with an AMD Ryzen 7 5700X3D (`8` cores), NVIDIA RTX 3080 (`10,240 MiB` reported by `nvidia-smi`), and `47.93 GiB` RAM. The Release candidate-detector folder currently contains only `paddle_text_detector_worker.py`; it has no packaged Python executable. The available system Python is `3.13.7` and has neither `paddle` nor `paddleocr`. An isolated research-only Python `3.12.10` venv outside the repository was subsequently created for the runtime evidence; it contains PaddlePaddle GPU `3.2.1` and PaddleOCR `3.7.0`, but none of those packages or model files were copied into a release artifact.
- 2026-08-02 per-candidate orchestration increment: `RunAllZonesAsync` creates one transient Tesseract/cache/translation/overlay work item per accepted candidate and publishes a ready candidate before a slow sibling from the same saved zone. `LiveTranslationSession` fingerprints each candidate crop separately; a changed or disappeared candidate cancels only its own work while completed unchanged siblings remain in the combined overlay. Deterministic fakes cover ready-before-slow publication, candidate disappearance, and candidate identity replacement. The transient candidates remain in-memory only and recursive detector invocation is disabled for their child work.
- 2026-08-02 runtime-and-geometry research on the local RTX 3080: isolated Python `3.12.10`, PaddlePaddle GPU `3.2.1` (CUDA `12.9`), and PaddleOCR `3.7.0` passed `pip check`, exposed `gpu:0`, and completed the real worker JSON protocol on S9/S10. The old transitive baseline remained unacceptable (`S9 10/10` with `1` extra; `S10 5/6` with `5` extras), even though warm detector P95 was `53.4 ms` and `97.5 ms` respectively. Reports are local-only under `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-track-d-adr-023\outputs\issue-42-gpu-runtime-baseline-2026-08-02`.
- 2026-08-02 bounded grouping research: a deterministic shared-core sequential grouping with a four-member cap and dense-top raw fallback avoids union-find/transitive connected-component merging. Its S9/S10 candidates plus existing Tesseract crop research and the CJK target post-filter retained exactly `S9 10/10` and `S10 6/6` with zero extras. On the held-out corpus it reached Japanese speech anchors `199/206` (`96.6%`), Tesseract non-empty crops `197/199` (`99.0%`), micro CER `10.7%` (`216/2025`), and Chinese tight lines `1179/1182` (`99.75%`). These are research-only results under `C:\Users\admin\Documents\Codex\2026-08-01\game-translator-track-d-adr-023\outputs\issue-42-bounded-shared-core-research-2026-08-02`; they do not waive packaging or latency gates.
- 2026-08-02 superseded cold-start gate: five clean worker launches with cached model files took `3502.1`, `3483.1`, `3447.7`, `3538.2`, and `3458.6 ms` to emit `ready`. This did not meet ADR-024's original `<= 2.0 s` gate. ADR-025, explicitly approved by the owner, changes only the opt-in pilot cold initialization-plus-first-result P95 gate to `<= 5.0 s`; it does not enable a default/UI path or waive the RTX 3060, packaging, or other release gates.
- 2026-08-02 cold-start phase follow-up: cached `PP-OCRv6_medium_det` launches spent about `1.65-1.73 s` importing Python/Paddle and `1.67-1.76 s` initializing the detector; first S9 results took `3.72-3.94 s`. The official lighter `PP-OCRv5_mobile_det` did not remove the runtime cost: its cached first S9 results were `3.59-3.85 s`. The current research host therefore meets the revised `<= 5.0 s` timing only provisionally; it is not the required RTX 3060 baseline. Python/Paddle startup optimization remains deferred final-stage work.
- Remaining before any enablement: repeat headless S9/S10 and held-out-corpus evidence on the RTX 3060 8 GiB baseline, including quality, masks, cold/steady P95, CPU/GPU/VRAM, packaged-runtime license, checksum, offline-install, and rollback checks before any default or UI exposure changes.
- 2026-08-02 validation after the ADR-025 timeout update: Release build completed with `0` warnings and `0` errors; focused Paddle detector tests passed `3/3`, and the full Release tests passed `402/402`; `tools/check-docs-mini.ps1` reported `0` markdown-link and `0` actionable-backtick problems. No UI window was opened.
- 2026-08-02 local-only package assembly (historical ADR-025 r5 package; removed locally with owner approval on 2026-08-18): it contained the published UI/Infrastructure output, portable Python `3.12`, PaddlePaddle GPU `3.2.1`, PaddleOCR `3.7.0`, bundled CUDA wheels, and only the `PP-OCRv6_medium_det` model. The app payload was `4,988.0 MiB` across `30,270` files; its `30,275` SHA-256 manifest entries were independently rechecked with zero mismatches. The ZIP was `3,104.9 MiB`, had `34,705` entries, and SHA-256 `d64764a6f892bb2659c64dcaef5599d7b4242bf1d863f8cffb7acb7908d3dacf`. A same-workspace headless packaged-worker S9 run returned `20` raw candidates with `3,766.4 ms` to ready and `4,891.5 ms` to the first result. This confirms the historical local package shape only; it is not an RTX 3060 P95 result or a release-enablement decision. The artifact included a distribution inventory, third-party notices, rollback instructions, and a headless verification report. No UI window was opened.
- 2026-08-02 packaging No-Go: the full research venv carries `10,426` `__pycache__` files (`141.4 MiB`) and a longest current-workspace path of `322` characters. The ZIP did extract at clean short path `C:\GT-adr025-r5`, but the first packaged worker missed the five-second readiness ceiling. A diagnostic run with a temporary `10 s` allowance reached ready only at `6,851.8 ms` and first S9 result at `7,940.0 ms` with `20` raw candidates. Therefore `r5` is checksum-valid assembly evidence but fails ADR-025's `<= 5.0 s` cold gate; do not promote it as an opt-in distribution artifact. A future pruned-runtime/clean-host experiment must meet the existing gate without raising it.
- 2026-08-08 pruned-runtime clean-path No-Go: the package builder now recreates the generated `candidate-detector` directory after publish, preventing a stale publish payload from bypassing its declared inventory. The resulting separate `C:\GT-adr025-r8-pruned` experiment excluded all `__pycache__`, Python `Doc`/headers/build tools/Tcl/standard-library tests, and `test`/`tests` site-package directories. It contained `13,856` files and `4,731.2 MiB` (about `256.8 MiB` below `r5`); its `13,855` SHA-256 entries rechecked with `0` mismatches. A fresh `D:\GT-adr025-r8-pruned-clean-20260808` install copy had `0` `__pycache__` directories before first start, but S9 did not emit `ready` within the unchanged `5 s` gate. A separate fresh diagnostic copy also did not emit `ready` within `10 s`. This proves that this cache-free pruning set is not a cold-start remedy; it remains research-only No-Go and must not be promoted or used to raise ADR-025's `<= 5.0 s` gate.
- The research artifacts remain transient candidate-bounds evidence inside a manual OCR zone. ADR-024 code adds no packaged runtime or new product dependency yet, and it does not modify `IOcrEngine`, persist profile data, or change Tesseract retry behavior.
- Do not exceed ADR-024: retain only transient candidates inside a saved OCR zone, keep Tesseract as the recognizer, do not change `IOcrEngine` or persisted profiles, and do not add an unconditional full-frame retry. Enablement remains off by default until every ADR-024 gate has release evidence.

### D4. ADR-028 readiness and bounded direct-provider delivery

Status: Done as historical readiness work; its retained safety/SLO constraints feed the active ADR-030 default.

- 2026-08-11: the owner accepted ADR-028 through Change Request [#45](https://github.com/Dementius-cell/Game-Translator/issues/45). It supersedes only ADR-025's cold-worker timing gate with a P95 `<= 5 s` first translated `OverlaySnapshot` SLO after verified readiness. All quality (with only the held-out non-empty-crop threshold later amended by ADR-029), package, safety, WPF-visible-render and disabled-by-default gates remain binding.
- The Application live-session path now has an opt-in readiness state (`Prewarming`, `Ready`, `Degraded`), a discarded detector/provider prewarm frame, direct `GoogleWeb` provider identity verification, bounded retry (at most three attempts with observable next-retry time), and a three-slot cache-miss candidate translation limiter. One-shot candidate execution is explicitly rejected when the readiness policy is requested, so it cannot bypass the barrier.
- Candidate source identity remains the publication authority: a changed/disappeared candidate and capture loss cancel/remove its overlay immediately; capture loss also invalidates readiness. A prewarm result invalidated during recovery is ignored. Tesseract remains mandatory for every retained candidate crop, and `WebAuto`, full-frame retry, default-provider substitution and legacy fallback are not used by this path.
- Focused verification passes for readiness/discarded-frame, bounded retry, capture-loss invalidation, late-old-result suppression, one-shot barrier rejection and three-region concurrency (`6/6`); the full Release suite passes `410/410`. The pilot is still not surfaced in UI/profile/default configuration.
- 2026-08-11 packaged-worker recovery integration: a headless opt-in `LiveTranslationSession` probe used the normal-bytecode-cache package worker and the original S9 image (`759x1080`, SHA-256 `7f3d41989bb39403df441ed2a72162c300dcae3bb98625cb84c6e84ee84fba2f`). After exact-PID termination of the task-owned worker, the live session reported `Ready -> Degraded` in `5.0 ms` with the adapter's explicit unavailable reason, then `Prewarming -> Ready` in `4,225.0 ms`, incrementing readiness generation `1 -> 2` and restart count `0 -> 1`. The prewarm frames published no overlay. Evidence: `work/persistent-worker-lifecycle/run-20260811-07-live-session/lifecycle-report.json`; it does not alter ADR-025's cold-start gate.
- 2026-08-11 direct-GoogleWeb cache-miss SLO repeat: with the same original S9 pixel source, persistent package detector, bounded grouping and existing Tesseract crop recognition, 30 of 30 measured first translated region overlays were valid. P95 was `1,164.4 ms` total (detector `240.2 ms`, Tesseract crop `901.4 ms`, direct GoogleWeb `176.9 ms`); the prewarm was excluded from the steady-state SLO and took `4,236.6 ms`. The runner constructed no `TranslationCache`, used no `WebAuto`, UI or fallback, and records the source SHA-256. Evidence: `work/real-translation-overlay/googleweb-s9-30-cache-miss-after-recovery-actual-source-20260811-report.json`. This is a headless `OverlaySnapshot` measure, not WPF physical-paint evidence.
- 2026-08-11 offscreen WPF visual-tree evidence: the original S9 pixels (same `759x1080` SHA-256) were rendered through the actual `OverlayWindow` XAML and its private view-model factory, with the production `WpfOverlayTextMeasurer`, by `RenderTargetBitmap`. In both `FitToSourceBounds` and `ExpandFromSourceCenter`, all `10/10` text items and `10/10` masks had visible white/dark rendered pixels; the window was never visible, `Window.Show()` was never called, and no HWND was created. Evidence: `work/wpf-overlay-render-visibility/run-20260811-04-sta-offscreen/wpf-overlay-render-report.json` with the two rendered composites. This closes neither ADR-028's physical WPF/compositor-paint gate nor any accessibility/user-readability review, because the current no-UI constraint deliberately excludes a shown-window run.
- 2026-08-12 physical WPF/compositor presentation (explicitly authorized by the owner): a local-only auto-closing probe invoked the actual `OverlayWindow.ShowSnapshot` for the original S9 pixels and showed it for `12,000 ms`. Before close, the real window had a non-zero HWND, `Window.IsVisible`, `IsWindowVisible(HWND)`, an `HwndSource` composition target, `Topmost`, capture exclusion, actual `1920x1080` size, and `10` text plus `10` mask items. Together with the prior offscreen `10/10` per-item pixel proof, this closes the physical shown-window/compositor-presentation sub-gate. Capture exclusion intentionally prevents a computer screenshot or an automation target; no bypass was attempted. This is not an accessibility, human-readability or game-background review. Evidence is local ignored harness source `work/wpf-overlay-render-visibility/Program.cs` and build-output report `work/wpf-overlay-render-visibility/bin/Release/net9.0-windows10.0.19041.0/physical-run-20260811-210844/wpf-overlay-render-report.json`; no capture, OCR, detector, translation or pilot session was started.
- 2026-08-11 held-out current-policy regression (local-only, ignored `work/**`): the normal-bytecode-cache package worker reran all 25 OpenMantra Japanese pages and 206 independent anchors through direct Paddle detection, bounded grouping, existing Tesseract crop recognition, the currently configured CJK target post-filter, and headless per-region overlay positioning. Geometry recall was `198/206` (`96.12%`) and CER `223/2018` (`11.05%`), with `0` overlay warnings, out-of-page scenarios and text overlaps; `187/198` mapped crops were non-empty (`94.44%`). Evidence: `work/direct-paddle-heldout-quality/openmantra-direct-default-filter-regression-20260811-1920.json`. This was below the original `>=98%` gate; ADR-029 now accepts this unchanged strict-filter result against its `>=94%` disabled-pilot baseline.
- A same-worker diagnostic without that optional CJK target post-filter passed the held-out recognizer thresholds (`198/206`, `196/198` non-empty or `98.99%`, micro CER `209/2018` or `10.36%`, and zero overlay failures): `work/direct-paddle-heldout-quality/openmantra-direct-unfiltered-regression-20260811-1921.json`. It remains a diagnostic only: relaxing the current filter to recover this corpus had already retained four extra S10 regions, so no product filter option was changed. ADR-029 accepts the current strict policy, not this unfiltered variant.
- A follow-up feature probe (local-only, ignored `work/**`) confirms that a simple Tesseract-word-confidence threshold is not a safe replacement policy: one known S10 extra had four words at `>=50` confidence, while an owner-matched held-out crop had non-empty text but no such word. Tuning a compound exception rule on this same held-out corpus would invalidate it as a regression gate, so no source configuration or filter behavior changed. Evidence: `work/candidate-filter-feature-probe/candidate-filter-features-20260811-1925.json` and its reproducible harness.
- A predeclared, source-disjoint 25-page OpenMantra split (`215` speech-bubble annotations) was then generated as calibration input: `work/candidate-filter-calibration/openmantra-calibration-manifest.json`. Its current-filter diagnostic reached `209/215` mapped (`97.21%`) with `197/209` non-empty (`94.26%`), CER `304/2129` (`14.28%`) and zero overlay failures. The upstream dataset README defines its `text` annotations as speech bubbles; visual review confirms that decorative SFX are absent from the reference while the current candidate pipeline can retain them. The split therefore aligns with the owner-required speech-region class and may be used only to select a policy; the fixed held-out corpus remains untouched until that policy is frozen.
- 2026-08-11 calibration scoring correction: the original 1:1 matcher was too strict for speech-bubble annotations because current bounded grouping can emit two vertical candidate segments inside one annotated bubble. A local many-to-one association allows each candidate to map to at most one bubble while allowing a bubble to own multiple segments. Under the current CJK filter, `209/215` bubbles have an associated candidate, `204/215` have every associated crop non-empty before filtering, `197/215` have every segment retained after filtering, four bubbles are multi-segment, and seven retained candidates remain outside every bubble. The visual gallery identifies those seven as SFX/decorative candidates. The earlier static threshold sweep is therefore diagnostic-only because it used the superseded 1:1 score; no source option or runtime filter changed. Evidence: `work/candidate-filter-calibration/candidate-filter-calibration-many-to-one-association-20260811.json` and `work/candidate-filter-calibration/calibration-default-filter-extra-candidates-20260811.png`; verified Issue `#41` correction: `https://github.com/Dementius-cell/Game-Translator/issues/41#issuecomment-5258268712`.
- 2026-08-11 bubble-enclosure probe No-Go: a research-only local grayscale connected-light-component cue tested whether the candidate sits inside an enclosed, sufficiently large light region. On the fixed calibration candidate set (`200` speech-associated, `7` unmatched SFX/decorative candidates), its best calibration-only variant achieved `189` TP, `4` FP, `3` TN and `11` FN (precision `97.93%`, recall `94.50%`, F1 `96.18%`). It rejects only three unmatched candidates while wrongly suppressing eleven speech candidates, so it is not a valid standalone retained-candidate filter. No source option, runtime filter, default, pilot or ADR changed. Evidence: `work/candidate-filter-calibration/speech-bubble-enclosure-feature-probe-20260811.json` and `work/candidate-filter-calibration/probe_speech_bubble_enclosure.py`.
- 2026-08-11 independent local classifier screen No-Go: a source-disjoint, predeclared 50-page OpenMantra calibration extension (`389` speech-bubble annotations) was run through direct Paddle detection, bounded grouping and existing Tesseract crops. The current filter yielded `376` retained candidates: `360` speech-associated and `16` unmatched. A local OpenCV random-forest screen trained only on the original 25-page calibration input (`200` speech-associated, `7` unmatched) suppressed just `8/16` unmatched candidates on the independent extension while wrongly suppressing `5/360` speech candidates at its best tested depth (`355` TP, `8` FP, `8` TN, `5` FN; recall `98.61%`). It is therefore not a safe retained-candidate policy and is rejected without testing or tuning on the fixed held-out corpus. No source option, runtime filter, default, pilot or ADR changed. Local-only ignored evidence: `work/candidate-filter-calibration/openmantra-extended-classifier-calibration-manifest.json`, `work/candidate-filter-calibration/candidate-filter-extended-classifier-features-20260811.json`, `work/candidate-filter-calibration/candidate-filter-extended-many-to-one-association-20260811.json`, `work/candidate-filter-calibration/speech-bubble-classifier-independent-evaluation-20260811.json`, and reproducible harness `work/candidate-filter-calibration/screen_speech_bubble_classifier.py`.
- 2026-08-12 owner quality decision: Change Request [#46](https://github.com/Dementius-cell/Game-Translator/issues/46) and accepted ADR-029 supersede only ADR-026 Gate 2's held-out non-empty mapped-crop threshold, from `>=98%` to `>=94%`. The accepted baseline is the unchanged strict CJK filter at `187/198` (`94.44%`); anchor recall `>=95%`, micro CER `<=15%`, S9 `10/10`, S10 `6/6`, zero retained extras, zero overlay warnings, mask safety and all disabled-pilot/default boundaries remain binding. This is an owner quality acceptance, not a runtime, configuration, source-policy or production-enablement change.
- 2026-08-11 release-hardening checkpoint (local-only; normal bytecode cache retained): a fresh candidate built beneath a deep `work/**` path failed its import probe with Windows `WinError 206` while loading a Paddle CUDA DLL. The same fresh package successfully built at short path `C:\GTRH3\candidate`, so the failure is a Windows path-length deployment constraint, not a change to the runtime or a cache-free-pruning retry. The short-path candidate's direct S9 packaged-worker probe passed `3,291.3 ms` ready and `4,436.2 ms` first result. Its `30,276` SHA-256 manifest entries independently rechecked with `0` mismatches both before and after the further checks. Its archive has `34,705` entries, is `3,104.9 MiB`, and SHA-256 `59a2b79eb3829cddfc662d1d2997424ba577277e283d568b8f1ff15a35bc47f3`. A clean offline extraction at `C:\GTRH3\offline` contained `30,270` application files, all of whose recorded hashes matched, and passed the packaged S9 worker probe in `3,562.0 ms` ready / `4,772.0 ms` first result. Five further sequential fresh-worker launches from that offline normal-cache copy all returned 20 candidates; their ready P95 was `3,377.7 ms` and first-result P95 was `4,492.1 ms`, both within the unchanged `<= 5 s` ADR-025 ceiling. A reversible rollback proof renamed only that disposable offline copy's `candidate-detector` directory, observed the expected unavailable error, then restored it. On the local RTX 3080 host, 50/50 steady S9 detections returned 20 candidates each with `72.6 ms` P95; the worker consumed `4.203` CPU seconds, peaked at `3.25 GiB` private bytes and `1.40 GiB` working set. Under WDDM, `nvidia-smi` exposed no per-process VRAM; the defensible global GPU baseline-to-peak observation was `1,843 -> 2,453 MiB` with `45%` sampled peak utilization. This is package/resource evidence only: it does not close the quality, physical WPF, clean-host, or pilot/default enablement gates. Local-only ignored measurement harness and report: `work/release-hardening/Measure-PackagedWorkerResources.ps1` and `work/release-hardening/candidate-r2-normal-cache-resource-report-20260811.json`.
- 2026-08-12 fresh-package/clean-root repeat (local-only, normal bytecode cache retained): a new unpruned candidate was built at disposable short path `C:\GTRH4\candidate` from the current dirty tree. Its independent pre-final SHA-256 pass verified 30,274 of 30,274 entries; after the packaged headless report and final ZIP, the final pass verified 30,277 of 30,277, with zero missing/mismatched files. The final ZIP has `34,707` entries, is `3,104.9 MiB`, and SHA-256 `8c6747a852b5aff379af245d21fdb8074cc3524a80823086130304ea83ac7a0d`. A new previously absent clean root `C:\GTRH4\offline-clean` unpacked the preceding equivalent package payload without network access; the first diagnostic packaged-worker run returned `20` candidates in `11,164.1 ms` to ready and `11,734.5 ms` to first result. Independent SHA-256 checks before and after the normal-cache evidence matched all `30,271` `app/**` files, so no payload mutation was retained. This is a same-host clean-root repeat, not a clean-host result.
- 2026-08-12 application-level prewarm/recovery P95 (local-only, fresh offline package): ten headless opt-in `LiveTranslationSession` cycles used exact-PID worker termination, the actual persistent packaged worker, bounded grouping, existing Tesseract crop recognition and direct GoogleWeb; no `TranslationCache`, `WebAuto`, fallback, UI, default or production enablement was used. Initial prewarm reached `Ready` at P95 `4,624.9 ms`, then first translated overlay after verified `Ready` at P95 `1,196.4 ms`; recovery reached `Ready` at P95 `3,987.0 ms`, then first translated overlay after recovered `Ready` at P95 `1,023.0 ms` (`5,010.0 ms` P95 from induced loss through the overlay). All `10/10` cycles recovered and published text plus masks. This meets ADR-028's `<=5 s` timing only after verified readiness; it does not reintroduce ADR-025's cold-start gate. Every initial/recovered first snapshot also emitted fit warnings for narrow candidate bounds, so this latency evidence is explicitly **not** a zero-overlay-warning quality pass. Local-only ignored harness/source and report: `work/persistent-worker-lifecycle/Program.cs`, `work/persistent-worker-lifecycle/run-recovery-overlay-series.ps1`, and `work/persistent-worker-lifecycle/run-20260812-gtrh4-series-03-final/recovery-overlay-p95-report.json`.
- 2026-08-12 expanded resource sample (local-only, same offline package): `120/120` steady direct detections returned `20` candidates, with P95 `75.7 ms`, mean `69.1 ms`, `8.906` worker CPU seconds over `18,829.8 ms`, `2.82 GiB` peak private bytes and `1.15 GiB` peak working set. Under Windows WDDM, per-process VRAM was not available; the defensible device-wide observation was `1,504 -> 2,133 MiB` (`+629 MiB`) and `46%` sampled peak GPU utilization. This is owner-accepted RTX 3080 proxy evidence only, not a generic RTX 3060 claim. Local-only ignored report: `work/release-hardening-fresh-20260812/offline-clean-120-resource-report.json`.
- 2026-08-12 narrow-candidate fit correction (local-only, disabled pilot path): temporary candidate zones now use `ExpandFromSourceCenter` only in memory, while the saved profile/default layout remains unchanged. `OverlayPlacementConstraints` bounds each translated item to its original manual capture zone and prevents it from covering sibling detector regions. The production WPF renderer adds a dark callout backplate and a connector only for these explicitly marked transient placements; ordinary overlays retain their prior presentation. No `IOcrEngine`, profile persistence, UI setting/default, legacy path, full-frame retry or enablement changed. Focused tests passed `46/46`.
- 2026-08-12 S9 candidate-layout evidence: an offscreen actual `OverlayWindow` render with production `WpfOverlayTextMeasurer` simulated the per-candidate crop geometry from the owner S9 pixels. The former crop-only baseline rendered `9/10` text items visibly and emitted fit warnings; adaptive candidate placement rendered `10/10` text and `10/10` masks visibly. A separate real headless `LiveTranslationSession` run with the existing normal-cache packaged worker, bounded grouping, Tesseract crop recognition and direct GoogleWeb produced `8` initial and `11` recovered translated items with `0` fit warnings after exact-PID worker recovery; after verified readiness its first overlays took `1,072.3 ms` initially and `960.0 ms` after recovery. Evidence is local ignored: `work/wpf-overlay-render-visibility/run-20260812-candidate-callout/wpf-overlay-render-report.json`, its two composite PNGs, and `work/persistent-worker-lifecycle/run-20260812-candidate-callout-single/lifecycle-report.json`. This is S9-only quality evidence, not a fresh package or production-enablement result.
- 2026-08-12 owner visual acceptance: the owner reviewed and accepted the adaptive S9 candidate-placement/callout result. This accepts the displayed S9 visual variant only; S10, held-out and clean-host gates remain open, and pilot/default/production enablement remains unauthorized.
- 2026-08-12 S9/S10 and held-out candidate-reflow evidence: the current source plus the normal-bytecode-cache packaged worker reran direct Paddle detection, bounded grouping, existing Tesseract crop recognition and headless candidate placement with the actual strict CJK target filter. S9 is `10/10` and S10 is `6/6`, each with zero retained extras, warnings and text overlaps (`work/quality-gates-20260812/s9-s10-actual-default-filter-quality.json`). The fixed 25-page OpenMantra held-out run used direct GoogleWeb translations for the actual Russian-card dimensions: `198/206` anchors (`96.12%`), `187/198` non-empty (`94.44%`), micro CER `11.05%`, and zero warnings, out-of-page items and text overlaps (`work/quality-gates-20260812/openmantra-heldout-real-googleweb-candidate-placement-default-filter-quality.json`). The reflow is restricted to combined transient candidate callouts; it does not modify ordinary overlay placement, profile/default configuration, `IOcrEngine`, legacy fallback or retry behavior.
- 2026-08-12 adversarial layout note: replacing every held-out translation with one identical long synthetic phrase produced one unresolved overlap on a 17-candidate dense page; the diagnostic emitted a warning and is a No-Go for that synthetic worst case. It is not used as quality acceptance. The actual direct-GoogleWeb held-out run above has zero warning/overlap and is the release-evidence result; future typography changes must retain both records.
- 2026-08-12 r4 fresh normal-cache package repeat: `work/release-hardening/release-candidates/v0.1.0-pre.20260812-adr028-r4-candidate-reflow-normal-cache` was built from the current dirty tree without cache-free pruning. Its five-second S9 packaged-worker check passed at `3,095.9 ms` ready and `4,345.6 ms` first detector result. Independent SHA-256 verification passed 30,275/30,275 entries before archive and 30,276/30,276 after archive, with no malformed, duplicate, missing or mismatched entries. The `3,104.9 MiB`, 34,705-entry ZIP SHA-256 is `764c53c18fe200ede14c188824e77615e05aa81e747911057f2a9e0b2fad9b80`.
- 2026-08-12 r4 operational repeat: a new previously absent offline root `D:\GTRH6` extracted the verified archive without a network-access command or UI, then returned `20` S9 candidates in `3,836.6 ms` ready / `4,490.9 ms` first result. Moving only its disposable app/candidate-detector directory to a recoverable sibling backup made the adapter report `Unavailable` with zero candidates, proving rollback. Ten headless opt-in S9 `LiveTranslationSession` prewarm/exact-PID-recovery cycles all recovered with zero debug warnings: initial ready P95 `4,425.4 ms`, initial translated overlay after Ready P95 `1,099.2 ms`, recovery ready P95 `3,581.4 ms`, recovery translated overlay after Ready P95 `987.5 ms` (`work/quality-gates-20260812/r4-s9-prewarm-recovery-p95/recovery-overlay-p95-report.json`). The 60-iteration packaged S10 detector resource run reported P95 `93.5 ms`, mean CPU `105.67%` of one core (`6.6%` of 16 logical processors), and Windows-WDDM device-wide GPU baseline-to-after-sequence delta `+650 MiB`; this RTX 3080 `10,240 MiB` host remains the owner-accepted RTX 3060 8 GiB proxy only (`work/quality-gates-20260812/r4-s10-resource-report.json`). Release build completed with zero warnings/errors, focused overlay tests passed 30/30, and full Release tests passed 412/412.
- 2026-08-13 owner verification acceptance: a genuinely separate clean Windows host and a further formal owner Go/No-Go checkpoint are unavailable. The owner accepts the r4 same-host clean-root evidence as a successful verification of this gate. `D:\GTRH6` remains accurately described as a new clean root on the same host, not as a second physical host; this is a documented gate acceptance, not an assertion that a separate-host run occurred.
- 2026-08-13 owner direction change: Change Request [#47](https://github.com/Dementius-cell/Game-Translator/issues/47) and accepted ADR-030 authorize the necessary default and composition changes. The quality-first target is now the normal one-shot and live path: GPU Paddle detection, bounded grouping, existing Tesseract crop recognition, the configured provider and per-region overlay. The live path keeps the persistent-worker/provider readiness barrier; regions publish progressively after their own work completes.
- 2026-08-13 implementation: `TranslationPipelineRunOptions.Default` now enables the candidate-region path, and the `MainViewModel` live path requires readiness. A detector/runtime failure returns a visible degraded result with no candidates; it does not silently select legacy full-page OCR/grouping, a full-frame retry, `WebAuto`, cached data or another provider. Legacy remains only behind an explicit diagnostic/compatibility option while later cleanup proves deletion safe. Tesseract and Windows OCR remain mandatory capabilities; no profile migration, public `IOcrEngine` change or direct-GoogleWeb default was introduced.
- 2026-08-13 r5 fresh normal-cache candidate package: `work/release-hardening/release-candidates/v0.1.0-pre.20260813-adr030-default-candidate-r5` was built from the same dirty tree without cache-free pruning. Its headless packaged S9 worker verification reached Ready in `2,791.4 ms` and its first result in `3,978.7 ms`; independent SHA-256 verification passed all 30,276 entries, including the final archive. The 34,705-entry, `3,104.9 MiB` ZIP SHA-256 is `695a64590f7e41cc98c98144e775df195801c820a273fefc24dff90185e95655`. The packaged UI DLL equals the current Release UI DLL. This is a locally verified release candidate, not a signed or externally published release.
- 2026-08-13 validation after the default transition: Release build completed with `0` warnings and `0` errors; focused profile/view-model tests passed `65/65`; the full Release suite passed `413/413`. No visible UI was opened, and no files were staged, committed or reverted.
- 2026-08-13 entry-point audit: the regular UI one-shot call and live session use the candidate path; `RunAsync` also uses candidate detection and no longer has an implicit full-zone OCR route under default options. A deterministic single-zone regression test verifies that the default flow sends only the bounded candidate crop to Tesseract. Legacy full-page behavior is selected only through the explicit `LegacyFullPage` diagnostic/compatibility option. After the guard and nullable-safe smoke resolver were added, Release build again completed with `0` warnings / `0` errors and the full suite passed `414/414`.
- 2026-08-15 live diagnostics/UI follow-up: Live start, stop and every distinct candidate-readiness degradation now save a privacy-filtered local report in the app-data live diagnostics directory. The report records only structured provider identifiers, failure categories and an HTTP code when available; it does not record provider response text or credentials. The current manual test reached controlled `GoogleWeb` provider prewarm degradation, not a Paddle detector failure. The UI now exposes `Open live reports`, removes the unpackaged-RC-only non-actionable update check, and labels the Windows OCR action accurately as help.
- 2026-08-15 r8 in progress (not finalized or archived): `work/release-hardening/release-candidates/v0.1.0-pre.20260815-adr030-provider-detail-ui-cleanup-r8` contains the live diagnostic/provider-detail and UI changes. Release build completed with `0` warnings / `0` errors; focused pipeline and view-model tests passed `108/108`. Its initial manifest independently verified `30,275` entries before the generated headless report; direct packaged-worker verification then returned `20` candidates (Ready `2,668.0 ms`, first result `3,622.1 ms`). Physical WPF/provider validation and final manifest/archive steps remain open.
- 2026-08-15 r9 in progress (not finalized or archived): `work/release-hardening/release-candidates/v0.1.0-pre.20260815-adr030-ocr-engine-validation-r9` normalizes the selected OCR-engine identifier before persisting or validating it and refreshes validation after language-pack Check/Install, eliminating the false mismatch where a trimmed check reports `Ready` but the editor still marks the engine invalid. Release build completed with `0` warnings / `0` errors; focused view-model tests passed `70/70`. Its initial manifest independently verified `30,275` entries before the generated headless report; direct packaged-worker verification returned `20` candidates (Ready `2,817.9 ms`, first result `3,950.8 ms`). Physical WPF/provider validation and final manifest/archive steps remain open.
- 2026-08-15 r11 metadata finalized and locally archived (not signed or published): `work/release-hardening/release-candidates/v0.1.0-pre.20260815-adr030-ocr-engine-visual-validation-r11` corrects the stale shell stage label to `Sprint 26` and replaces the OCR-engine control's stale WPF validation adorner with explicit model-driven error presentation. A physical WPF launch of this exact candidate showed `Sprint 26` and a neutral `Tesseract` border for the valid profile; no language pack was installed during the check. Release build completed with `0` warnings / `0` errors and the full suite passed `420/420`. Its normal-cache worker returned `20` candidates from the original S9 (`759x1080`, SHA-256 `7f3d41989bb39403df441ed2a72162c300dcae3bb98625cb84c6e84ee84fba2f`) in Ready `2,800.1 ms` / first result `3,989.7 ms`; final manifest verification passed `30,277` entries. ZIP: `34,707` entries, `3,104.9 MiB`, SHA-256 `bdde7624bd51a24f1b14e4504aca49dd35e61ebcaae40b3cb19d772b9209d066`. A new same-host clean-root ZIP extraction had 30,271/30,271 matching `app/**` files and its worker returned `20` candidates in Ready `3,137.0 ms` / first result `4,221.3 ms`. This is same-host clean-root evidence, not physical clean-host evidence. ROLLBACK.md directs reinstallation of the preceding verified package; the retained r5 rollback archive hash was independently rechecked as `695a64590f7e41cc98c98144e775df195801c820a273fefc24dff90185e95655`.
- 2026-08-15 retired disposable same-host clean-root payloads: `D:\\GTRH5` and `D:\\GTRH6` were confirmed unused by the current r5–r9 candidates and removed at the owner's request. Their historical same-host clean-root evidence remains historical; this deletion does not affect r5, r6, r7, r8 or r9.
- 2026-08-15 r12 manual real-content verification: the separate active profile `Paddle Live EN-RU Full Screen` used direct `GoogleWeb`, `en -> ru`, Tesseract `eng`, and one saved `1920x1080` (`0,0,1,1`) zone. The automatically saved report game-translator-live-live-stopped-20260815-124727-849-0003.txt records candidate pipeline `Ready`, generation `1`, restart count `0`, no unavailable reason, and `74` text overlays with `74` source masks. The owner manually observed a manga page rather than a headless/test harness and reported near-target screenshot recognition with nearly all visible text found. This is a successful same-host manual functional run; it does not by itself finalize r12, add signing/publication, or replace the remaining release metadata/archive and offline-install evidence.
- 2026-08-15 r12 finalized locally (not signed or published): `work/release-hardening/release-candidates/v0.1.0-pre.20260815-adr030-paddle-candidate-grouping-r12` has a final manifest verified at 30,279/30,279 entries. Its `34,711`-entry, `3,106.8 MiB` ZIP has SHA-256 `bb9d6ee2682efe29d4d39c21d13039f2eaa753fb3f43939e224542265e45672e`; the listed archive checksum was independently recomputed. The packaged UI DLL equals the current RID Release UI DLL. A newly created same-host clean-root ZIP extraction at `work/release-hardening/offline-r12-same-host-clean-root` matched all 30,273/30,273 `app/**` SHA-256 entries. Its packaged worker returned `20` candidates from the preserved S9 input in Ready `2,919.8 ms` / first result `3,912.5 ms`; after a reversible runtime-directory removal it reported the expected missing python.exe, and after exact restoration it again returned `20` candidates (Ready `2,900.5 ms`, first result `3,846.1 ms`). This is same-host clean-root verification, not a physical clean-host result.
- 2026-08-15 same-host manual CJK functional verification: only the disposable `work/release-hardening/offline-r12-same-host-clean-root` copy received `jpn.traineddata` and `chi_sim.traineddata`; the finalized r12 directory and ZIP were not changed. The automatically saved Japanese report game-translator-live-live-stopped-20260815-135600-127-0003.txt records candidate pipeline `Ready`, restart count `0`, no unavailable reason, `37` recognized OCR blocks and `10` overlay text/mask items. The Chinese report game-translator-live-live-stopped-20260815-135827-130-0006.txt likewise records `Ready`, restart count `0`, no unavailable reason, `23` recognized OCR blocks and `10` overlay text/mask items. The owner manually reports that vertical Japanese and Chinese text produced an almost ideal result. This is same-host manual visual evidence, not a signed/public release or a replacement for formal language-specific quality gates.
- 2026-08-15 r13 was locally finalized with four bundled models but was superseded before release: selecting Thai from the GoogleWeb language list exposed that `th` was not mapped to Tesseract `tha`. With explicit owner approval, the obsolete r13 directory and its same-host clean-root were permanently deleted; its old integrity evidence remains historical only and r13 must not be used as a release candidate.
- 2026-08-15 r14 was locally finalized but is superseded by r15: the owner Thai visual run showed that the profile sent Tesseract model code `tha` to GoogleWeb instead of Google language tag `th`. The pipeline was Ready and OCR produced `52` blocks, but cache evidence recorded `38` `tha -> ru` entries with `29` unchanged texts. r14 remains an historical local build and must not be used for Thai-to-Russian verification.
- 2026-08-15 r15 local release candidate (historical; superseded by r18 and removed locally with owner approval on 2026-08-18): it separated GoogleWeb source/target tags from Tesseract OCR model codes. Source/target UI options contain only the `104` GoogleWeb tags; OCR options retain Tesseract models. Compatibility normalization changes legacy `tha` to `th` before cache and provider requests, while explicit OCR `tha` remains unchanged. Source Release build completed with `0` warnings / `0` errors and full tests passed `452/452`. The candidate bundled exactly `chi_sim`, `eng`, `jpn`, and `tha`; its packed worker passed headless verification on local calibration evidence with Ready `2,862.5 ms`, first result `3,969.4 ms`, and `24` candidates. Independent SHA-256 verification passed all `30,280` manifest entries. ZIP CRC verification passed with `30,281` files and `4,437` directories; ZIP: `34,718` entries, `3,069.5 MiB`, SHA-256 `be811f36fdb0f83afa3e81045fef47d079f6b47e9efabec18bde9a45c81ce782`. The packaged UI DLL matched the RID-specific Release publish output at that time. The archive and candidate were local build outputs, never an externally published release.
- 2026-08-15 reproducible runtime source published to `main` as `59f128a`: `tools/bootstrap-paddle-runtime.ps1`, `tools/verify-paddle-runtime.ps1` and pinned lock/requirements reproduce the CPython/Paddle/model/Tesseract runtime from official distributions and verify hashes plus GPU/model readiness. A new same-host clean-root completed bootstrap, Release build (`0` warnings / `0` errors) and `445/445` tests; the primary worktree passed `453/453`. The compatibility facade for an older grouping test was added without publishing calibration assets or OCR-content test material. This source publication did not sign or publish r15; that historical local candidate was later superseded and removed with owner approval.
- 2026-08-16 writing-system grouping work started under parent Issue `#48` with cohort Issues `#49`–`#55`. The local source now resolves OCR language tags and Tesseract traineddata codes to seven writing-system profiles before bounded Paddle-candidate grouping. The first narrow `ComplexSouthEastAsian` rule covers Thai, Lao, Khmer and Myanmar: it accepts a `40%` shared horizontal overlap for ragged adjacent dialog-line boxes and at most `8` adjacent lines, while every other profile retains the global `6`-line cap. Global vertical-gap and dense-layout safeguards stay unchanged. Deterministic synthetic tests cover all seven profile mappings, a synthetic geometry smoke case for each profile, the Thai geometry cases, separation of a distant bubble and service wiring; they contain no real page/image/text material. Verification: focused tests `39/39`; Release build `0` warnings / `0` errors; full Release tests `480/480`; `tools/check-docs-mini.ps1` reported `0` Markdown-link and `0` actionable-backtick problems. This is uncommitted local source work, not a signed or published release.
- 2026-08-16 r16 Thai-grouping test candidate was superseded and removed after manual Thai evidence: its live report showed a single nearby Thai dialog split into `6 + 2` candidate crops, proving the shared six-line cap was the cause after the `40%` overlap rule had already applied. The report remains local under the app-data live diagnostics directory; r16 was not signed, published or archived.
- 2026-08-16 r17 Thai eight-line grouping test candidate was superseded and removed after manual Thai evidence. The owner reported improvement but some bubbles remained split; the latest stopped-session report has no grouped candidate with more than seven OCR lines, so the residual issue is not evidenced as the eight-line cap. It was not signed, published or archived.
- 2026-08-16 residual Thai-split diagnostics: candidate grouping now preserves the original Paddle member bounds through crop recognition and writes a privacy-filtered `Candidate grouping geometry` section to each local live report. It records candidate identifier, source-member count and coordinates only, not OCR or provider text. Release build completed with `0` warnings / `0` errors; focused grouping/view-model tests passed `110/110`, and the full Release suite passed `480/480`. This remains uncommitted local source work.
- 2026-08-16 seven-cohort synthetic grouping smoke coverage: each fixed writing-system cohort now has an explicit representative smoke scenario: LTR spaced, CJK horizontal/hybrid, CJK vertical, complex South-East Asian, Brahmic/Indic, RTL Hebrew and RTL Arabic-derived. Every scenario asserts resolver selection, bounded ordinary dialog grouping and retained source-member geometry; it also asserts that only the complex South-East Asian cohort receives the approved ragged-line tolerance and eight-line cap while every other cohort remains at the global six-line bound. The cases use only synthetic rectangles and language tags, with no real page, image, OCR or provider text. Release build completed with `0` warnings / `0` errors; full Release tests passed `494/494`. This is uncommitted local source work.
- 2026-08-16 r18 Thai grouping-geometry diagnostic candidate assembled locally (not signed, published or archived): `work/release-hardening/release-candidates/v0.1.0-pre.20260816-adr030-thai-grouping-geometry-diagnostics-r18`. It retains r17 behavior, normal Python bytecode cache, pinned GPU Paddle runtime and exactly `chi_sim`, `eng`, `jpn`, `tha` Tesseract packs. The packaged worker returned Ready in `3,010.8 ms`, first result in `4,228.1 ms`, and `24` candidates on the existing local detector input. Final metadata plus independent SHA-256 verification passed all `30,339` entries; packaged UI DLL equals the current RID-specific Release UI DLL. It exists only to collect geometry from the owner-approved manual live run; no ZIP/archive was created. The superseded unarchived r17 directory was removed after checking that no process referenced it, returning the release-candidate free space to `43.25 GiB`.
- 2026-08-16 local-only original-image smoke coverage was added under ignored `work/writing-system-image-smoke`. It runs the actual r18 Paddle detector, candidate filtering and writing-system bounded grouping against one fixed real image for each of the seven cohorts; it deliberately excludes OCR, translation, UI, cache and provider output. The local manifest records source, licence, SHA-256 and conservative count floors, but no OCR/provider text. The rerun passed all seven profiles with raw/bounded candidate counts: LTR `108/89`, CJK horizontal `62/34`, CJK vertical `28/13`, complex South-East Asian `41/12`, Brahmic/Indic `74/17`, RTL Hebrew `31/4`, RTL Arabic-derived `33/7`; every retained region and its source members stayed in-image and the resulting groups did not overlap. Inputs are local copies from the Apache-2.0 MDPBench subset, the pre-existing OpenMantra Japanese corpus, and the CC-BY-4.0 Pinkas Hebrew dataset. Images, manifests and JSON reports remain ignored local evidence and must not be committed or published.
- 2026-08-17 curated local-only visual pass: `work/writing-system-overlay-smoke` ran the actual packaged r18 detector, bounded cohort grouping, Tesseract crop recognition, direct GoogleWeb and production `OverlayWindow` XAML rendered offscreen on seven compact review images. The ignored manifest records source URLs, stated-or-unasserted licence status and SHA-256; its JSON report contains only counts, timings, paths, hashes and failure categories, while source/derived PNGs stay local. All seven completed the technical chain without a provider/renderer failure: English `21/10/8/8`, Chinese vertical `8/5/1/1`, Japanese vertical `28/13/12/12`, Thai `7/5/5/5`, Hindi `2/1/1/1`, Hebrew `6/1/1/1`, Arabic `14/5/4/4` (raw/grouped/recognized/rendered). This is deliberately not a product-quality pass: human visual review still treats the Japanese manga result as the useful baseline; the Chinese vertical mask, Arabic placement and non-scene Hebrew input require follow-up before any language-cohort quality claim. No source image, OCR text, translation text, model or derived overlay is eligible for Git publication.
- 2026-08-18 owner direction: visual release review now prioritizes the product domains — selected-language game UI, manga and books — rather than outdoor scene text. `work/writing-system-overlay-smoke/product-domain-unit-visual-manifest.json` is ignored/local-only and contains those six scenarios; mixed-language full-screen UI is retained only as a robustness probe because running it under one selected-language profile creates irrelevant cross-script candidates. The actual r18 chain completed on English game UI `2/2/2/2`, Thai dialogue `3/1/1/1`, Japanese manga `28/13/12/12` and English book `2/2/2/2`; small constructed Japanese game-prompt `6/3/0/0` and Chinese manga-bubble `2/2/0/0` fixtures produced candidates but no recognized text. There was no fallback or substituted result. Those two fixtures are therefore diagnostic font cases, not a product-quality failure or visual gate; future language-quality examples must be real game/manga/book captures retained locally, while no visual input, OCR/provider text, model or generated PNG may be committed.
- 2026-08-18 real product-domain source selection: an ignored local corpus now contains actual English, Japanese, Chinese and Thai game-dialogue screenshots; the existing local OpenMantra page remains the real Japanese manga case and the Apache-2.0 MDPBench page the real English book case. Each input is pinned by URL, conservative licence note and SHA-256 in `work/writing-system-overlay-smoke/real-product-domain-manifest.json`; none is eligible for Git. The complete packaged r18 path on those inputs yielded English game `5/5/5/5`, Thai game `7/4/4/4`, Japanese manga `28/13/12/12`, and English book `108/89/89/89` (raw/grouped/recognized/rendered). Japanese horizontal game chat `11/9/0/0` and Chinese horizontal game choice `3/3/0/0` found candidates but returned no Tesseract crop text; the book page rendered with a visibility/bounds warning because its dense text became 89 groups. These are concrete runtime limitations to address with target-domain evidence, not a reason to conceal failure, add a fallback or treat scene-text success as product readiness. The generated comparison and JSON report are local-only under `work/writing-system-overlay-smoke/reports/real-product-domain-r18-20260818`.
- 2026-08-18 horizontal CJK game-dialogue fix: the enabled CJK target post-filter had imposed its vertical `height >= width` geometry rule on every Japanese/Chinese request, rejecting horizontal dialogue after Tesseract had returned it. The filter now applies only to vertical OCR, retaining the existing vertical guard and adding no retry, fallback or changed OCR engine. The new unit regression proves a horizontal Japanese candidate is retained while the prior vertical rejection test remains. The local actual-r18 detector/full pipeline rerun at `work/writing-system-overlay-smoke/reports/cjk-horizontal-game-postfilter-fix-20260818` passed Japanese game `11/9/9/9` and Chinese game `3/3/3/3` (raw/grouped/recognized/rendered); overlays are in bounds and visible. Full Release test suite: `495/495`; Release build: `0` warnings, `0` errors. Inputs, reports and images remain local-only.
- 2026-08-18 CJK narrow-rule regressions: source tests now cover JA and ZH horizontal candidates escaping the vertical-only CJK geometry filter, while JA and ZH vertical candidates retain that rejection guard; the writing-system resolver explicitly maps horizontal `zh-CN` to the CJK horizontal/hybrid profile. A local-only three-case product regression manifest at `work/writing-system-overlay-smoke/cjk-ui-manga-regression-manifest.json` completed the actual r18 Paddle-worker path with no full-page fallback: Japanese horizontal game UI `11/9/9/9`, Chinese horizontal game UI `3/3/3/3`, and Japanese vertical OpenMantra manga `28/13/12/12` (raw/grouped/recognized/rendered). It does not alter the configured provider or its default; source Release tests passed `498/498`, with Release build `0` warnings and `0` errors. Inputs, reports and overlays are local-only and excluded from Git.
- 2026-08-18 owner direction for books: do not extend game-dialogue grouping or overlay rules to solve the dense English book-page result (`108/89/89/89`, with a visibility/bounds warning). Book support is a separate product decision: an explicit `Book` mode would need its own paragraph-level grouping contract, overlay policy, profile/UI exposure, measurable page-oriented regressions and approval/change record before implementation. Until then, the candidate path remains scoped to dialog/comic behavior; no full-page fallback, provider-default change or hidden book heuristic is authorized.
- 2026-08-22 owner-approved Content Layout Mode decision: Change Request `#56` and accepted ADR-031 define one per-zone policy seam for candidate grouping, candidate overlay layout and minimum live refresh interval. The additive profile field defaults to the only accepted `DialogComic` mode, which preserves automatic bounded writing-system grouping, centered per-region overlay and every-cycle live refresh. Application OCR requests, transient candidate overlays, pipeline identity and live scheduling now consume the policy; the zone editor exposes the mode and its effective behavior while the nonfunctional candidate-path overlay expansion control is no longer shown. Missing JSON fields remain schema-`1.0` compatible. This does not implement `Book` or `StaticMenu`, change provider defaults, or add any fallback. Verification: focused tests `128/128`, full Release tests `506/506`, Release build `0` warnings / `0` errors, docs mini-check `0` link and `0` actionable-backtick problems. Issue: `https://github.com/Dementius-cell/Game-Translator/issues/56`.
- 2026-08-22 tabbed editor implementation and visual parity follow-up: the persistent left profile rail remains the only profile navigation surface; the right workspace has `Zones & OCR`, `Translation`, `Overlay`, `Live & Diagnostics`, and `Hotkeys & Settings` with compact shared profile details and Save/Reset outside the tabs. The old native-gray tab strip and long legacy page shell were replaced by the approved light card layout, teal selected-tab indicator, readable four-action profile toolbar and large primary Live action. The single Start/Stop/Open-reports block remains Live-only; `ContentLayoutMode` remains per zone, while the common OCR language-pack checklist moved from the zone page to a bounded-height list in Hotkeys & Settings. Automatic `start-live`, `stop-live-requested`, and `live-stopped` report creation was not changed. The first built executable exposed a runtime-only WPF error because `Run.Text` inferred TwoWay for read-only `ActiveProfileName`; it was corrected to explicit OneWay and a no-window STA test now creates, measures and lays out the real `ShellView`. No WPF window was launched by the verification. Release build completed with `0` warnings / `0` errors and the full Release suite passed `519/519`.
- 2026-08-22 compact editor interaction follow-up: the right workspace now has a dedicated sixth `OCR Packs` tab, and `Hotkeys & Settings` no longer embeds the global pack checklist. Profile cards support inline rename on double-click with Enter/Escape handling. The fixed logical zone surface is rendered in a smaller preview and supports direct LMB zone movement in addition to empty-space creation and corner resizing; the former Up/Down actions are labeled Earlier/Later with processing-order tooltips. Translation compatibility normalizes historical generic `zh` to provider tag `zh-CN`, while explicit Tesseract OCR codes remain unchanged. Overlay settings have a separate helper row and bounded width, and shared controls/cards use denser readable spacing. No provider default, candidate grouping, overlay runtime policy, live debug autosave, legacy fallback or full-frame retry changed. Verification remained headless/no-window: Release build completed with `0` warnings / `0` errors, the full Release suite passed `525/525`, and the docs mini-check reported zero Markdown-link and zero actionable-backtick problems.
- 2026-08-22 responsive workspace follow-up: the Zones workspace no longer reserves a blank right-hand area or renders a second zone list. Its preview and OCR-preprocessing cards wrap according to available width, and parameters below follow the zone selected directly on the preview. The Translation card is bounded and hides endpoint/project/region/token fields for experimental web providers that do not consume stored credentials; official-provider credential editing remains available. Start/Stop Live are duplicated in the shared header through the existing commands while the full Live controls and automatic diagnostics remain in their tab. A no-window STA layout regression now cycles all six tabs at `1024x640`, `1280x720`, `1600x900`, `1920x1080`, and `2560x1440`, checks horizontal bounds, and verifies the Zones cards wrap below `1600` while sharing a row at wider sizes. No OCR/pipeline/provider default or persistence policy changed. Release build completed with `0` warnings / `0` errors and the full Release suite passed `527/527`.
- 2026-08-22 live stale-overlay diagnostics follow-up: automatic live reports now retain a bounded privacy-safe candidate lifecycle trace. It links capture and detector timing, grouped candidate/member geometry, crop-revision cancellation/removal, repeated candidate work, stable-text window timing, OCR-to-translation group counts, failures by stage, and each candidate-snapshot publication. The trace deliberately contains no frame pixels, OCR text, translated text, provider response or credential. It preserves the current ADR-030 path and changes no timing, grouping, retry, fallback or provider behavior. Focused pipeline/diagnostics tests passed `42/42`; Release build completed with `0` warnings / `0` errors and the full Release suite passed `527/527`. No UI window was opened.
- 2026-08-22 r20 local diagnostic candidate assembled (not signed, published or archived): `work/release-hardening/release-candidates/v0.1.0-pre.20260822-live-lifecycle-diagnostics-r20`. It packages the current lifecycle-trace source, normal Python bytecode cache, pinned GPU Paddle runtime and `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs; `jpn_vert` is included for the already-supported vertical Japanese path. The packaged worker passed headless verification on existing local detector input with Ready `3,170.8 ms`, first result `4,802.9 ms`, and `24` candidates. Final metadata plus independent SHA-256 verification passed all `30,340` manifest entries; packaged UI DLL equals the current RID-specific Release publish output. No UI window was opened.
- 2026-08-22 live candidate jitter and diagnostic-retention follow-up: live reconciliation now preserves a candidate identity only for a deterministic one-to-one match that has the same grouped-member count, IoU `>= 0.95`, and every outer edge within `4 px` of its original discovery bounds. The discovery anchor prevents accumulated small moves from attaching to another region. Candidate OCR/crop identity and the existing text-stability gate remain the authority for work; geometry matching alone never reuses a translation after changed pixels. The trace adds an explicit `CandidateGeometryJitterMatched` event. Lifecycle events are delivered incrementally to the UI and retained with a queue capacity of `131072`, avoiding a full-trace allocation on every live refresh. Each automatic diagnostics report is hard-limited to `99,000,000` UTF-8 bytes; if exceeded, it preserves the report header and newest tail with a visible truncation marker. Focused candidate and diagnostics checks passed `7/7`; no UI window was opened.
- 2026-08-22 r21 local diagnostic candidate assembled (not signed, published or archived): `work/release-hardening/release-candidates/v0.1.0-pre.20260822-live-jitter-retention-r21`. It packages the bounded jitter-matching and diagnostics-retention source with the normal Python bytecode cache, pinned GPU Paddle runtime and `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The packaged worker passed headless verification on existing local detector input with Ready `3,332.6 ms`, first result `4,694.2 ms`, and `24` candidates. Final metadata plus independent SHA-256 verification passed all `30,340` manifest entries; the packaged UI DLL equals the current RID-specific Release publish output. The candidate contains `10,426` Python bytecode-cache files, is `5,004.9 MiB`, and has no archive. No UI window was opened.
- 2026-08-22 `glhf` provider follow-up: the latest local live report showed no provider request or HTTP `429`; candidate readiness rejected the selected non-`GoogleWeb` provider before detector/OCR/translation work began. A separately selectable, credentialless `glhf` direct-provider mode now uses ScreTran-compatible Google GTX request semantics: `GET /translate_a/single`, `client=gtx`, `sl=auto`, configured target language and segment concatenation. It is an explicit direct mode, not a default-provider change; it performs no fallback, full-frame retry, cache/provider substitution or quota bypass. Candidate readiness accepts only direct `GoogleWeb` or explicitly selected `glhf`, and verifies the response provider identity. A real `429` remains surfaced as a throttled provider failure. Focused provider, credential, DI, UI and readiness tests passed `29/29`; no UI window was opened.
- 2026-08-22 ScreTran Yandex/Bing compatibility follow-up: ScreTran `1.3.1` references `GTranslate 2.2.8` and calls `YandexTranslator`/`BingTranslator` with only the target language, allowing each direct provider to detect source text. `YandexWeb` now uses the same one-request Android-form translate endpoint with a six-minute `ucid`, `srv=android`, `format=text`, and target-only `lang`, instead of scraping SID pages. `BingWeb` now extracts only key/token from its translator page, caches them to their one-hour key expiry, uses a fresh GUID as `IG`, fixed `IID=translator.5024.1`, `fromLang=auto-detect`, the configured target-language hot-patches, and detects an in-band `statusCode`. Direct Yandex/Bing requests no longer refresh a session or retry after a failure. This preserves no-default-change and no-direct-provider-fallback/retry policy; explicit `WebAuto` behavior was not changed. Focused experimental-provider checks passed `12/12`; no UI window was opened.
- 2026-08-22 Windows worker-IPC hardening: the managed persistent worker host now explicitly uses UTF-8 without a preamble for all redirected streams. The Python worker defensively accepts a proper UTF-8 BOM and the two known legacy Windows mojibake spellings before parsing its first JSON request, and returns the precise exception type/message through the existing local detector diagnostics if a request fails. The release builder now reads the worker source explicitly as UTF-8 before copying it into the packaged runtime. This changes neither detector geometry nor OCR, translation, fallback, retry or provider policy. Release build completed with `0` warnings / `0` errors and the full Release suite passed `533/533`; no UI window was opened.
- 2026-08-22 owner-authorized local retention cleanup: obsolete unarchived candidates `r18`, `r19`, `r20` and `r21` were removed only after confirming no candidate process was active. No source, calibration artifact, runtime cache or local visual corpus was removed. The four candidate directories occupied about `22.55 GiB`; free space on `D:` increased from about `90.2 GiB` to `110.0 GiB`.
- 2026-08-22 r22 local verification candidate assembled and finalized (not signed, published or archived): `work/release-hardening/release-candidates/v0.1.0-pre.20260822-scretran-direct-providers-r22`. It packages the current source, normal Python bytecode cache, pinned GPU Paddle runtime and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. Packaged headless worker verification passed on the existing local detector input: Ready `2,972.1 ms`, first result `4,249.1 ms`, `24` candidates. Final metadata plus independent SHA-256 verification passed all `30,340` manifest entries; the packaged UI and Infrastructure assemblies match their corresponding current RID-specific publish outputs, and packaged worker source matches the current UTF-8 source. No UI window, ZIP/archive, signing, staging, commit, push or publication was performed.
- 2026-08-22 readiness-barrier removal: the candidate pipeline no longer sends a test translation before live work, restricts the configured provider to `GoogleWeb`/`glhf`, or rejects `YandexWeb`/`BingWeb` before detector, OCR and translation. The profile-selected provider is now called only for actual OCR candidate groups; there is no default change, provider substitution, fallback or retry. Candidate availability remains diagnostics-only and does not block a subsequent scheduled capture after a worker loss. Release build completed with `0` warnings / `0` errors; full Release tests passed `531/531`. No UI window was opened and no candidate package was built for this source-only change.
- 2026-08-22 r23 local readiness-barrier-removal candidate assembled and finalized (not signed, published or archived): `work/release-hardening/release-candidates/v0.1.0-pre.20260822-readiness-barrier-removal-r23`. It packages the current source with normal Python bytecode cache, pinned GPU Paddle runtime and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. Packaged headless worker verification passed on the existing local detector input: Ready `3,235.9 ms`, first result `4,533.7 ms`, `24` candidates. Final metadata plus an independent SHA-256 verification passed all `30,340` manifest entries; the packaged UI assembly matches the current RID-specific Release publish output. No UI window, ZIP/archive, signing, staging, commit, push or publication was performed.
- 2026-08-22 r23 live-log analysis and follow-up: ten owner live sessions recorded working vertical Chinese/Japanese and horizontal English/Thai candidate paths with `0` `CandidateWorkFailed` events. The two Thai/vertical trials had zero candidates before OCR/provider work, while a later Thai/horizontal session translated `122` candidate works with no failure. The diagnostics also showed `51` bounded-grouping changes, mostly in English, after some earlier candidate overlay publications. Live work now waits for a second matching bounded-grouping observation before starting crop OCR/translation whenever the existing stable-text live policy is enabled; the lifecycle trace records `CandidateGroupingAwaitingConfirmation`. This adds one live detector cycle before the existing OCR-text stability window and does not add a fallback, retry, cache/provider substitution or default-provider change. OCR orientation is now automatically fixed to Horizontal and its selector disabled whenever any effective zone OCR language lacks a bundled vertical layout; only Japanese and simplified/traditional Chinese keep vertical selection. The explicit, non-automatic `Small source (heavy)` preprocessing preset provides `3x` scaling, strong contrast/sharpening, thresholding and noise reduction for tiny source text. Release build completed with `0` warnings / `0` errors, focused tests passed `151/151`, and the full Release suite passed `541/541`. No UI window was opened and no candidate package was built for these source changes.
- 2026-08-22 r24 local portable self-contained candidate assembled and finalized (not signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260822-portable-self-contained-r24`. It packages the current source, self-contained `win-x64` .NET 9 host, normal Python bytecode cache, pinned GPU Paddle runtime, model, and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The packaged worker verification passed on the existing local detector input: Ready `3,078.5 ms`, first result `4,465.2 ms`, `24` candidates. Final metadata identifies the self-contained target; independent SHA-256 verification passed all `30,729` entries. The verified transfer archive is `GameTranslator-v0.1.0-pre.20260822-portable-self-contained-r24-win-x64.zip`, `3,169.5 MiB`, SHA-256 `f03d131aa6511cd1d5ea883e3f3f73c49c139294353ecb48afab67d9d5766fab`, with `35,168` entries. No UI window was opened. A compatible Windows x64 system and NVIDIA driver remain external host prerequisites; no .NET, Python, Paddle model or selected OCR-pack download is required at runtime.
- 2026-08-22 r24 portable startup defect and r25 correction: a local WER/.NET Runtime event proved r24 exited before UI startup with `FileNotFoundException` for `WindowsBase, Version=9.0.0.0`. `PublishInfrastructureCompositionModule` had copied the complete self-contained Infrastructure build over the UI publish output, replacing the WPF `WindowsBase.dll` runtime assembly (`9.0.0.0`, `2,238,248` bytes) with the `Microsoft.NETCore.App.Ref` reference assembly (`4.0.0.0`, `16,208` bytes). The target now copies only the composition module, its SQLite/Tesseract/WinRT dependencies, native `x64`/`x86` Tesseract payload and detector worker. The release builder additionally rejects a self-contained candidate unless its `WindowsBase`, `PresentationCore`, `PresentationFramework` and `System.Xaml` identities match the declared WindowsDesktop major version. Release build completed with `0` warnings / `0` errors and the full Release suite passed `541/541`. r25 was assembled and finalized at `work/release-hardening/release-candidates/v0.1.0-pre.20260822-portable-windowsdesktop-runtime-fix-r25`; its static WPF gate passed with `WindowsBase 9.0.0.0`, and its packaged GPU worker verification passed with Ready `3,113.2 ms`, first result `4,398.5 ms`, `24` candidates. Independent SHA-256 verification passed all `30,726` entries. Its transfer archive is `GameTranslator-v0.1.0-pre.20260822-portable-windowsdesktop-runtime-fix-r25-win-x64.zip`, `3,167.6 MiB`, SHA-256 `9f039ddc76657399e9cb7bb5175ab3e395d166485ae8335ce0e9b2e7e6f5c6ac`, `35,163` entries. No UI window was opened; r24 remains retained only as a defective local artifact and must not be used for launch testing.
- 2026-08-22 r25 portable startup defect and r26 correction: the next owner launch report showed `ReflectionTypeLoadException` while creating the external Infrastructure module: the package carried `Microsoft.Extensions.DependencyInjection.Abstractions` and `Microsoft.Extensions.Logging.Abstractions` `9.0.0.0` from UI while `GameTranslator.Infrastructure` references both `10.0.0.0` through its Tesseract dependency. The local source Release uses `10.0.0.0` for both because the ordinary composition copy supplies Infrastructure's dependency closure. The selective publish target now explicitly supplies those two matching assemblies. The self-contained builder verifies every direct `GameTranslator.Infrastructure` assembly reference against the candidate's physical assembly identity, in addition to the WPF runtime gate. Release build completed with `0` warnings / `0` errors; the full Release suite passed `541/541`. r26 is finalized at `work/release-hardening/release-candidates/v0.1.0-pre.20260822-portable-external-module-closure-fix-r26`; its GPU worker verification passed with Ready `2,874.9 ms`, first result `4,082.0 ms`, `24` candidates, while independent SHA-256 verification passed all `30,725` entries and the full direct Infrastructure-reference closure. No UI window was opened. Do not use r24/r25 for launch testing; r26 awaits owner launch verification before making another transfer archive.
- 2026-08-22 r26 Tesseract native-layout defect and r27 correction: owner live diagnostics showed that r26 started and its detector reached Ready, but every candidate failed at OCR before any translation or overlay. The external composition publish target had flattened `x64`/`x86` Tesseract native files into the application root even though TesseractOCR resolves them from architecture directories. The target now preserves architecture-specific x64 and x86 subdirectories and rejects root-level native duplicates. OCR-stage lifecycle diagnostics record bounded, single-line immediate and deepest inner-exception type/message without frame pixels, OCR/translated/provider text or credentials. The release builder requires `eng.traineddata` and a successful hidden `GameTranslator.UI.exe --portable-ocr-smoke` run from the assembled `app` directory; the synthetic frame and recognized text are not persisted. Release build completed with `0` warnings / `0` errors and the full Release suite passed `544/544`. r27 is finalized at `work/release-hardening/release-candidates/v0.1.0-pre.20260822-portable-tesseract-closure-fix-r27`: packaged Tesseract OCR passed with `1` block / `10` characters in `290.4 ms`, packaged GPU worker passed with Ready `2,943.7 ms`, first result `4,293.5 ms` and `24` candidates, and independent SHA-256 verification passed all `30,728` entries. The package retains `10,426` Python bytecode-cache files; no UI window, transfer archive, signing, staging, commit, push or publication was performed. r27 remains a local RC pending owner physical launch/live verification.
- 2026-08-23 live stabilization and grouping-controls follow-up (source only, no new RC): the configured live timing presets now require matching bounded-group observations and revision-scoped normalized OCR observations before any translation call (`Fast 2/2`, `Balanced 3/3`, `Conservative 4/4`) while retaining their existing polling/minimum-time values. A grouping/member or source revision cancels in-flight candidate work, clears its OCR-stability state and is revalidated before publication. Exact translation-cache keys use deterministic keyed locks so overlapping identical misses coalesce without serializing unrelated text. Privacy-safe lifecycle diagnostics now record the actual last-start timing/preprocessing/provider selection, required/observed confirmation counts, cache hit/miss/store counts and provider request start/completion timestamps without OCR, translation or response text. Horizontal Auto grouping retains the writing-system primary caps (`6`, or `8` for complex South-East Asian scripts) but permits a strictly aligned, at-most-`12 px` continuation up to `10` lines; per-zone hard limits (`1..12`) are available as maximum horizontal lines or maximum vertical columns according to OCR orientation, and old schema `1.0` JSON defaults to Auto. OCR preprocessing labels are now distinct from live timing: Light, Standard, Strong and Tiny source - heavy. Release build completed with `0` warnings / `0` errors and the full Release suite passed `551/551`; no visible UI, packaging, signing, staging, commit, push or publication was performed.
- 2026-08-23 r28 local portable candidate assembled and finalized (not signed, published, archived or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260823-live-stability-grouping-r28`. It packages the current live-stability/grouping-controls source, self-contained `win-x64` .NET 9 host, normal Python bytecode cache, pinned GPU Paddle runtime/model and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The mandatory hidden Tesseract smoke executed from the assembled `app` directory and passed with `1` block / `10` characters in `297.7 ms`; packaged GPU Paddle verification passed with Ready `2,798.7 ms`, first result `3,799.0 ms` and `24` candidates. Final metadata plus an independent SHA-256 audit passed all `30,728` manifest entries with no missing, mismatched or extra file; the package contains `30,729` files, `10,426` ordinary Python bytecode-cache files and is `5.019 GiB`. No visible UI window, transfer archive, deletion of older candidates, signing, staging, commit, push or publication was performed. The bundled runtime came from the established same-host clean-root bootstrap and is not physical-clean-host evidence.
- 2026-08-23 experimental-provider retirement follow-up (source only): with explicit owner approval, the aggregate `WebAuto` fallback provider and direct `glhf` alias were removed from their runtime implementations, dependency-injection registration, credentialless-provider metadata and UI choices. Direct `GoogleWeb` remains available alongside separately selected `BingWeb` and `YandexWeb`; the official Google/Azure/Yandex set and provider default are unchanged. Existing profiles containing `WebAuto` or `glhf` remain readable without silent migration, but the editor reports that the provider is no longer supported and requires an explicit replacement before running. Historical ADR/evidence references were retained as historical records. Release build completed with `0` warnings / `0` errors, focused provider/credential/UI/composition tests passed `106/106`, the full Release suite passed `551/551`, and the docs mini-check reported no link or actionable-backtick errors. No visible UI, packaging, signing, staging, commit, push or publication was performed.
- 2026-08-23 r29 local portable candidate assembled, finalized and archived (not signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260823-direct-web-provider-cleanup-r29`. It packages the provider-retirement source as a self-contained `win-x64` application with the pinned GPU Paddle runtime/model, normal Python bytecode cache and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The mandatory hidden Tesseract smoke from the packaged `app` directory passed with `1` block / `10` characters in `328.4 ms`; packaged GPU Paddle verification passed with Ready `2,793.1 ms`, first result `3,923.1 ms` and `24` candidates. The packaged UI and Infrastructure assemblies match their current RID Release outputs. Independent SHA-256 verification passed all `30,729` manifest entries with no malformed, missing, mismatched or extra file; the app contains `30,722` files, including `10,426` ordinary Python bytecode-cache files, and is `5.014 GiB`. The verified transfer archive is `GameTranslator-v0.1.0-pre.20260823-direct-web-provider-cleanup-r29-win-x64.zip`, `3,170.6 MiB`, SHA-256 `d6d534148e47a49554da7eb168ab1cf7837d90e19b077d8ec87bb06c69ac9d42`, with `35,167` entries. No visible UI window was opened; this remains same-host package evidence, not physical-clean-host evidence.
- 2026-08-23 owner-authorized release-candidate cleanup: after r29 passed packaged Tesseract/Paddle and full manifest/archive verification, the obsolete r15 ZIP, r22-r28 candidate directories, r23 build logs and r28 ZIP were permanently removed from `work/release-hardening/release-candidates`. Eleven items were removed, returning `47.54 GiB`; only r29 remains in that directory and `D:` has about `104.49 GiB` free. Source files, application logs, calibration artifacts/local visual corpus and the pinned bootstrap runtime were not removed. No signing, staging, commit, push or publication was performed.
- 2026-08-25 first live-performance package (roadmap 18.1 items 1-3, source only): synchronous native Tesseract recognition now runs outside the caller through a process-wide three-slot bounded executor while retaining a separate disposable engine per request, the public `IOcrEngine`, OCR/preprocessing/grouping output and r29 stability counts. `CandidateWorkStarted` is recorded before the candidate chain can enter OCR. Candidate and legacy work completion now signals a separate serialized publication operation, so the UI can revision/source-revalidate and publish completed overlays before the next detector poll; detector cadence no longer adds the complete refresh duration on top of its configured interval. Revision change, candidate disappearance and session cancellation cannot publish a detached late result, and concurrent candidates still publish progressively. Candidate reconciliation no longer creates the unused full-zone `FrameFingerprint`; byte-exact crop fingerprints remain the source authority. Focused Tesseract tests passed `42/42`, the lifecycle-order test `1/1`, pipeline tests `51/51`, copy/revalidation checks `3/3`, and live view-model tests `5/5`; Release build completed with `0` warnings / `0` errors and the full Release suite passed `561/561`. No visible UI, portable RC, signing, staging, commit, push or publication was performed. Byte-identical OCR reuse and detector prewarm remain gated and unchanged.
- 2026-08-25 r30 local portable candidate assembled, finalized and archived for owner live-performance verification (not signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260825-live-performance-optimization-r30`. It packages roadmap 18.1 items 1-3 as a self-contained `win-x64` application with the pinned GPU Paddle runtime/model, normal Python bytecode cache and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The mandatory hidden Tesseract smoke from the packaged `app` directory passed with `1` block / `10` characters in `292.7 ms`; packaged GPU Paddle verification passed with Ready `2,790.2 ms`, first result `3,871.2 ms` and `24` candidates. The packaged UI, Application, Infrastructure and Domain assemblies match their current RID-specific Release outputs. Independent SHA-256 verification passed all `30,729` manifest entries; the manifest and disk each contain exactly `30,729` non-manifest files with no malformed, duplicate, missing, mismatched or extra entry. The app contains `30,722` files, including `10,426` ordinary Python bytecode-cache files, and is `5.014 GiB`. The verified transfer archive is `GameTranslator-v0.1.0-pre.20260825-live-performance-optimization-r30-win-x64.zip`, `3,170.6 MiB`, SHA-256 `32499c2f7c533d323fd4e84491fbc25c96020793f3954c34582af8607098e1db`, with `35,167` entries. No visible UI window was opened. An interrupted pre-manifest assembly was retained separately as `v0.1.0-pre.20260825-live-performance-optimization-r30-partial-aborted-20260825-204530` and must not be used for testing. r30 remains same-host packaging evidence; owner live latency/allocation/CPU/GC smoke is the next evidence step, while roadmap items 4-5 remain gated and unchanged.
- 2026-08-27 r30 owner-log grouping-stability correction (source verified, replacement RC pending): the fresh post-r30 reports contained no `CandidateWorkFailed` event, and all `631/631` non-empty completed candidate works still produced exactly one translation-input group, so neither bounded grouping composition nor the post-OCR translation grouping showed a direct algorithmic split regression. The performance package did expose a cadence/stability coupling: detector throughput rose from roughly `2.99-3.76 Hz` in the available pre-r30 reports to `7.42-8.52 Hz`, while the unchanged count-only gate reduced median candidate discovery-to-work-start time from `398-992 ms` to `112-250 ms`. In the seven Fast vertical-Japanese sessions, `41/349` translated candidates were regrouped or removed within `500 ms`. Product timing presets already promised a minimum stable interval, so candidate grouping now requires both the existing `2/3/4` matching observations and the corresponding Fast/Balanced/Conservative wall-clock duration of `200/250/320 ms`; a grouping revision resets both count and elapsed duration. Crop source identity, OCR text stability, provider/cache behavior, revision/source revalidation and the grouping algorithm itself are unchanged. Privacy-safe lifecycle diagnostics now record grouping first/last observation time, observed duration and required duration. The existing nine-parameter `TranslationPipelineRunOptions` constructor signature remains intact; the new validated duration is an additive init property. Focused pipeline tests passed `55/55`, timing/report tests `2/2`, live stop/cancel tests `5/5`; Release build completed with `0` warnings / `0` errors and the full Release suite passed `564/564`. No visible UI, signing, staging, commit, push or publication was performed. Roadmap items 4-5 remain gated and unchanged.
- 2026-08-27 r31 replacement portable candidate assembled, finalized and archived for owner grouping/performance verification (not signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260827-grouping-stability-duration-r31`. It packages the r30 performance work plus the wall-clock grouping-stability correction as a self-contained `win-x64` application with the pinned GPU Paddle runtime/model, normal Python bytecode cache and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The hidden packaged Tesseract smoke passed with `1` block / `10` characters in `308.5 ms`; packaged GPU Paddle verification passed with Ready `3,037.9 ms`, first result `4,186.4 ms` and `24` candidates. Packaged UI, Application, Infrastructure and Domain assemblies match their current RID-specific Release outputs. Independent SHA-256 verification passed all `30,729` manifest entries, and exact manifest-to-disk comparison found no malformed, duplicate, missing, mismatched or extra entry. The app contains `30,722` files, including `10,426` ordinary Python bytecode-cache files, and is `5.014 GiB`. The verified transfer archive is `GameTranslator-v0.1.0-pre.20260827-grouping-stability-duration-r31-win-x64.zip`, `3,170.6 MiB`, SHA-256 `956190291d45fd63ffc34a67aea2d308661fa3fb2c8432ba2befc9c3a6dbbf26`, with `35,167` entries. No visible UI window was opened. r31 supersedes r30 for further owner testing; r30 remains retained as the evidence candidate that exposed the cadence/stability coupling. This is same-host packaging evidence, not the required owner live comparison or physical-clean-host evidence.
- 2026-08-29 r31 owner-log vertical-Japanese repetition correction (source verified; packaged in r32 below): the local privacy-safe report named game-translator-live-live-stopped-20260829-081206-778-0009.txt showed that the visible repetition was not repeated publication or multiple translator calls. One detector candidate with horizontal `508x38` geometry was processed under explicit Vertical Japanese OCR, Tesseract returned `12` OCR blocks, and WholeZone correctly joined them into one translation-input group and one translated overlay. The already-enabled vertical CJK post-filter rejected this geometry in `TextCandidateRegionOcrService.RecognizeAsync`, but the optimized direct per-candidate batch/live path bypassed that post-OCR check. Both optimized paths now apply the same existing filter after crop OCR and before stability/grouping/cache/provider work; rejected candidates produce no translation request or overlay. No text deduplication, OCR mode change, grouping/stability count or duration change, source/revision relaxation, provider fallback or default change was introduced. The two new wide/repeated-result batch/live regressions passed `2/2`; focused candidate OCR/pipeline tests passed `68/68`; Release build completed with `0` warnings / `0` errors and the full Release suite passed `566/566`. No visible UI, signing, staging, commit, push or publication was performed. Roadmap items 4-5 remain gated and unchanged.
- 2026-08-29 r32 replacement portable candidate assembled, finalized and archived for owner vertical-Japanese verification (not signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260829-vertical-cjk-postfilter-r32`. It packages r31 plus the restored vertical CJK post-OCR guard as a self-contained `win-x64` application with the pinned GPU Paddle runtime/model, normal Python bytecode cache and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The hidden packaged Tesseract smoke passed with `1` block / `10` characters in `296.7 ms`; packaged GPU Paddle verification passed with Ready `2,779.6 ms`, first result `3,878.5 ms` and `24` candidates. Packaged UI, Application, Infrastructure and Domain assemblies match their current applicable Release/RID outputs. Independent SHA-256 verification passed all `30,728` pre-archive manifest entries with no malformed, duplicate, missing, mismatched or extra file; the archive is the final `30,729`th non-manifest entry. The app contains `30,722` files, including `10,426` ordinary Python bytecode-cache files, and is `5.014 GiB`. The independently rehashed transfer archive is `GameTranslator-v0.1.0-pre.20260829-vertical-cjk-postfilter-r32-win-x64.zip`, `3,170.6 MiB`, SHA-256 `f5ea862d113b5520a51360ceeee1509dcaaf6ae74888ca08e78f319467e13d96`, with `35,167` unique entries and all required app/runtime/language-pack paths. No visible UI window was opened. r32 supersedes r31 for the reported repetition check; this remains same-host packaging evidence, not physical-clean-host evidence.
- 2026-08-29 r32 owner retest exposed a second vertical-Japanese repetition cause (source corrected; replacement RC pending): the newest privacy-safe live report confirmed that the restored CJK guard rejected the wide candidates it targets, while four valid tall candidates each still produced one translation-input group and one translated overlay. Read-only cache inspection linked the screenshot to one `YandexWeb` persistent-cache entry whose non-repeating 16-element CJK source had a 30-word translation made from the same six-word unit exactly five times; its creation timestamp matched the single provider request in the preceding r31 report. Translation cache output now applies a narrow `YandexWeb`-only sanitizer on memory hits, persistent hits and provider misses: it collapses only an exact full token-sequence repetition of at least five copies when the normalized source is not itself an exact repetition. Four copies, intentional repeated sources and every other provider remain unchanged; corrected provider misses are persisted, while old poisoned rows are corrected on read without requiring cache deletion. Privacy-safe lifecycle diagnostics expose only the sanitized-output count. Focused cache plus end-to-end overlay tests passed `15/15`, all pipeline tests passed `58/58`, the live-report smoke passed `1/1`; Release build completed with `0` warnings / `0` errors and the full Release suite passed `574/574`. No visible UI, signing, staging, commit, push or publication was performed. Roadmap items 4-5 remain gated and unchanged.
- 2026-08-29 r33 replacement portable candidate assembled, finalized and archived for owner vertical-Japanese verification (not signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260829-yandex-repetition-sanitizer-r33`. It packages r32 plus the narrow `YandexWeb` exact-repetition sanitizer as a self-contained `win-x64` application with the pinned GPU Paddle runtime/model, normal Python bytecode cache and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The hidden packaged Tesseract smoke passed with `1` block / `10` characters in `304.2 ms`; packaged GPU Paddle verification passed with Ready `2,838.6 ms`, first result `4,072.0 ms` and `24` candidates. Packaged UI, Application, Infrastructure and Domain assemblies match their current applicable Release/RID outputs. Independent SHA-256 verification passed all `30,728` pre-archive manifest entries with no malformed, duplicate, missing, mismatched or extra file; the archive is the final `30,729`th non-manifest entry and its independently recomputed hash matches its sole manifest record. The app contains `30,722` files, including `10,426` ordinary Python bytecode-cache files, and is `5.014 GiB`. The verified transfer archive is `GameTranslator-v0.1.0-pre.20260829-yandex-repetition-sanitizer-r33-win-x64.zip`, `3,170.6 MiB`, SHA-256 `0519691c735d31a76ba62699e6e04f0327b5fb0fad5b53b5edf77a46d5169cff`, with `35,167` unique entries, no duplicate or unsafe path and all required app/runtime/language-pack paths. No visible UI window was opened. r33 supersedes r32 for the reported cached provider-output repetition; this remains same-host packaging evidence, not physical-clean-host evidence.
- 2026-08-29 r33 owner-log source-order correction (source verified; replacement RC pending): the newest privacy-safe report named game-translator-live-live-stopped-20260829-092854-707-0003.txt recorded `182/182` non-empty completed candidate works with exactly one translation-input group and no failures, so it did not show a grouping split. Geometry-to-cache correlation did expose a latent reading-order defect in the final vertical-Japanese multi-column candidates: OCR columns at X `[55,12]`, `[58,10]` and `[144,97,53]` were sent left-to-right instead of Japanese right-to-left. `TranslationTextGroupingService` now selects orientation-aware ordering through the existing writing-system profile: `CjkVertical` columns are clustered by horizontal alignment, ordered right-to-left, and read top-to-bottom inside each column; the previous horizontal row ordering is unchanged for other profiles. Translation grouping mode, stability counts/durations, OCR filtering, cache/provider behavior and revision/source revalidation are unchanged. A focused grouping regression first reproduced the old wrong order, the focused grouping/candidate/pipeline set passed `75/75`, the updated full-app `jpn_vert` smoke passed `1/1`, Release build completed with `0` warnings / `0` errors, and the full Release suite passed `576/576`. No visible UI, signing, staging, commit, push or publication was performed. Roadmap items 4-5 remain gated and unchanged.
- 2026-08-29 r34 replacement portable candidate assembled, finalized and archived for owner vertical-Japanese reading-order verification (not signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260829-vertical-cjk-reading-order-r34`. It packages r33 plus the orientation-aware vertical-CJK source ordering as a self-contained `win-x64` application with the pinned GPU Paddle runtime/model, normal Python bytecode cache and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The hidden packaged Tesseract smoke passed with `1` block / `10` characters in `295.2 ms`; packaged GPU Paddle verification passed with Ready `2,870.8 ms`, first result `4,063.1 ms` and `24` candidates. Packaged UI, Application, Infrastructure and Domain assemblies match their current applicable Release/RID outputs. Independent SHA-256 verification passed all `30,728` pre-archive manifest entries with no malformed, duplicate, missing, mismatched or extra file; after archive creation, the manifest and disk each contain exactly `30,729` non-manifest entries. The app contains `30,722` files, including `10,426` ordinary Python bytecode-cache files, and is `5.014 GiB`. The independently rehashed transfer archive is `GameTranslator-v0.1.0-pre.20260829-vertical-cjk-reading-order-r34-win-x64.zip`, `3,170.6 MiB`, SHA-256 `ff18860cd689cf469853e44e1b15add5461f6ad425e4fd40468f6f2ae8b9d2ff`, with `35,167` unique entries, no case-insensitive duplicate or unsafe path and all required app/runtime/language-pack paths. No visible UI window was opened. r34 supersedes r33 for owner testing; this remains same-host packaging evidence, not physical-clean-host evidence.
- 2026-08-29 owner-authorized local build cleanup: after r34 passed packaged Tesseract/Paddle, manifest, assembly and archive verification, the obsolete r29, both r30, r31, r32 and r33 release-candidate directories plus `12` reproducible project `bin`/`obj` directories were permanently removed. The six old candidates occupied `45.511 GiB` and the generated project outputs about `952.1 MiB`; observed free space on `D:` increased from `38.15 GiB` to `84.99 GiB`. Only r34 remains under `work/release-hardening/release-candidates`. Source files, the pinned bootstrap runtime, AppData diagnostics/cache, calibration artifacts and local evidence were retained. The removed local build outputs are not recoverable, while their historical metadata remains recorded here. No signing, staging, commit, push or publication was performed.
- 2026-08-29 r34 owner retest exposed a pre-OCR vertical-CJK cap split; adaptive implementation is documented but not started. The privacy-safe report named game-translator-live-live-stopped-20260829-162952-479-0003.txt retained `4,478` events with no dropped event or candidate failure. All `46/46` non-empty completed works still produced exactly one translation-input group and translated block, but a five-column source was emitted as a four-column candidate plus an adjacent one-column candidate. Across all five members the shared vertical overlap was `116/116` (`1.0`) and the final horizontal gap was `2 px`, so the fifth column met the existing geometry gates and was excluded only by `MaximumVerticalGroupMembers = 4`. Because the vertical seed is currently left-to-right, the split isolates the rightmost starting column for Japanese reading order. The owner directed an adaptive `CjkVertical`-only Auto model: no fixed member count, deterministic right-to-left neighbor evaluation, whole-group coherence/discontinuity cuts, and the existing numeric per-zone limit retained only as an explicit override. Horizontal/non-CJK behavior, OCR/grouping stability, revision/source revalidation and provider/cache policy remain out of scope. Detailed local context and gates are in `work/adaptive-cjk-vertical-grouping-handoff-20260829.md`; roadmap section 18.3 records the tracked implementation boundary. No source implementation, visible UI, packaging, signing, staging, commit, push or publication was performed.
- 2026-08-29 adaptive vertical-CJK Auto grouping implemented and source-verified; owner live acceptance and any replacement portable remain pending. A failing regression first reproduced the exact r34 five-column `4 + 1` cap split. `CjkVertical` Auto now evaluates immediate columns right-to-left without a fixed member count and cuts on whole-group overlap, width/alignment coherence and adjacent-gap discontinuity; explicit `MaximumVerticalColumns = 1..12` still uses the prior hard-limit path. Horizontal and all non-`CjkVertical` profiles retain their prior semantics. Regressions cover `2/5/8/12` coherent columns, adjacent bubbles, ragged/staggered/noise/mixed geometry, all six non-vertical-CJK profiles and explicit override. Live diagnostics add only bounded ordered raw/group-member rectangles, total counts, SHA-256 geometry fingerprints and resolved profile/orientation; they record no OCR, translated or provider-response text. Focused grouping/candidate/pipeline/profile tests passed `157/157`; Release build completed with `0` warnings / `0` errors and the full Release suite passed `594/594`. Ignored headless geometry-only corpora passed all seven profiles: OpenMantra Japanese retained `28/13/28` raw/bounded/represented candidates and RCTW Chinese retained `8/5/8`, with all bounds in-image and no overlapping groups. Byte-identical S9/S10 source inputs are no longer present at their recorded temporary paths or retained evidence roots, so those two runs were not repeated and same-size rendered composites were explicitly rejected by SHA-256 rather than substituted. The docs mini-check found `0` Markdown-link and `0` actionable-backtick problems, and `git diff --check` reported no whitespace errors. No visible UI, portable build, signing, staging, commit, push or publication was performed; roadmap items 4-5 remain unchanged.
- 2026-08-29 r35 adaptive vertical-CJK grouping portable candidate assembled, finalized and archived for owner live smoke (not signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260829-adaptive-cjk-vertical-grouping-r35`. It packages the source-verified adaptive `CjkVertical` Auto path as a self-contained `win-x64` application with the pinned GPU Paddle runtime/model, normal Python bytecode cache and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The hidden packaged Tesseract smoke passed with `1` block / `10` characters in `311.9 ms`; packaged GPU Paddle verification passed with Ready `2,908.4 ms`, first result `4,195.4 ms` and `24` candidates. Packaged UI, Application, Infrastructure and Domain assemblies are byte-identical to their current applicable Release/RID outputs. Independent pre-archive SHA-256 verification passed all `30,728` manifest entries with no malformed, duplicate, unsafe, missing, mismatched or extra file; after archive creation the manifest and disk each contain exactly `30,729` non-manifest files. The app contains `30,722` files, including `10,426` ordinary Python bytecode-cache files, and is `5.014 GiB`. The independently rehashed transfer archive is `GameTranslator-v0.1.0-pre.20260829-adaptive-cjk-vertical-grouping-r35-win-x64.zip`, `3,170.6 MiB`, SHA-256 `96ffc6b41c3d9fcc7ddbcac9c4dc81587dcd1c3c96bfc942acfbe36ff42b7ce0`, with `35,167` entries, no case-insensitive duplicate or unsafe path and all required app/runtime/model/language-pack paths. No visible UI window was opened. r35 supersedes r34 for the adaptive-grouping owner smoke; this remains same-host packaging evidence, not owner live acceptance or physical-clean-host evidence.
- 2026-08-29 r35 owner live-smoke repetition and overlay-flicker correction implemented and source-verified; a replacement portable remains pending explicit owner instruction. The privacy-safe report named game-translator-live-live-stopped-20260829-201046-450-0003.txt retained `5,707` events with no dropped event, candidate failure, pipeline restart or unavailable transition. Read-only cache/event correlation found a fresh `YandexWeb` miss whose non-repeating seven-element source produced a ten-word translation with one nine-word consecutive repeated run plus one other word; the existing exact-full-sequence sanitizer therefore did not match it and the provider result was persisted. The narrow Yandex-only sanitizer now also collapses a dominant consecutive single-word run of at least five copies only when at most one other translated word exists; two other words, four repeats, intentionally repeated sources and all other providers remain unchanged. The same report recorded `233` overlay publications, including `47` empty snapshots and `13` non-empty-to-empty-to-non-empty transitions, with no overlay hide/restore-for-capture events. Live candidate publication now keeps an already displayed non-empty snapshot while the authoritative current batch is transiently empty and candidate replacement/grouping/stability work is pending; it does not republish or revalidate the old revision. The replacement still publishes only after the existing geometry/source/revision checks, while confirmed disappearance, definitive empty OCR, failure or capture loss publishes the current empty snapshot and removes the old overlay. Focused cache/pipeline tests passed `78/78`; Release build completed with `0` warnings / `0` errors and the full Release suite passed `598/598`. Diagnostics remain text-free and expose only bounded geometry, counts, timing and fingerprints; adding OCR/translation/provider text was not included without a separate explicit default-off retention/redaction decision. The exact former five-tall-column owner geometry did not recur in this smoke, so adaptive grouping still lacks that specific owner-live acceptance evidence. No visible UI, portable build, signing, staging, commit, push or publication was performed; roadmap items 4-5 remain unchanged.
- 2026-08-29 owner-authorized local text diagnostics follow-up: the owner confirmed that the diagnostic reports are for personal debugging and must not be uploaded. Candidate-completion lifecycle events now retain three independently labelled bounded text sequences for the automatic filesystem report: ordered OCR blocks, the actual grouped translation inputs, and final translated output after cache/sanitizer processing. Each sequence retains at most `16` entries and each entry is normalized to one line and at most `512` UTF-16 code units without splitting a surrogate pair; the existing `99,000,000` UTF-8 report limit and newest-tail truncation remain in force. Raw provider/HTTP responses, credentials and frame pixels are still excluded, and the application contains no diagnostics upload path. This is a diagnostics-only change: OCR/grouping, provider/cache selection, stability, revision/source revalidation, cancellation and overlay publication semantics are unchanged. Focused pipeline/live-report tests passed `67/67`; Release build completed with `0` warnings / `0` errors and the full Release suite passed `599/599`. No visible UI, portable build, signing, staging, commit, push or publication was performed.
- 2026-08-29 dense-interface follow-up: the shared WPF spacing scale and all repeated local workspace intervals were compacted without changing commands, bindings, validation or tab ownership. Normal controls now use `30 px` minimum height, primary buttons `32 px`, compact buttons `26 px`, cards `10/8 px` padding, common vertical gaps `4-6 px`, and tabs `10x7 px` padding in a `36 px` strip. The profile sidebar narrowed from `344` to `280 px`; outer workspace padding changed from `32,24,32,28` to `18,12,18,16`; top/status chrome changed from `64/40` to `40/24 px`. The zone surface/preprocessing cards are `540/500 px`, the 16:9 surface preview is `490x275.625`, the OCR-pack list is `360 px` high and the live capture preview is `140 px`. Typography remains at readable desktop sizes (`22 px` workspace title, `18 px` profile/top title, `16 px` section title, `13 px` tabs/primary actions). Source layout regressions passed `19/19`; the no-window WPF measure/arrange regression passed at `1024x640`, `1280x720`, `1600x900`, `1920x1080` and `2560x1440`; Release build completed with `0` warnings / `0` errors and the full Release suite passed `600/600`. No visible UI, portable build, signing, staging, commit, push or publication was performed.
- 2026-08-30 r36 unpacked portable candidate assembled and finalized for owner live smoke (not archived, signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260830-live-smoke-diagnostics-dense-ui-r36`. It packages the r35 repetition/flicker corrections, owner-authorized bounded local text diagnostics and dense-interface follow-up as a self-contained `win-x64` application with the pinned GPU Paddle runtime/model, normal Python bytecode cache and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The hidden packaged Tesseract smoke passed with `1` block / `10` characters in `304.8 ms`; packaged GPU Paddle verification passed with Ready `2,730.9 ms`, first result `3,794.0 ms` and `24` candidates. Packaged UI, Application, Infrastructure and Domain assemblies are byte-identical to their current applicable Release/RID outputs, and the packaged worker payload equals the current source after the release builder's established UTF-8 BOM. Independent SHA-256 verification passed all `30,728` manifest entries with no malformed, case-insensitive duplicate, unsafe, missing, mismatched or extra file. The app contains `30,722` files, including `10,426` ordinary Python bytecode-cache files, and is `5.014 GiB`; the complete candidate contains `30,729` files and is `5.019 GiB`. No r36 ZIP/archive was created, no packaged process remains, and no visible UI window was opened. The immediately preceding source verification remains Release build `0` warnings / `0` errors and full Release suite `600/600`; this is same-host packaging evidence pending owner live acceptance.
- 2026-08-30 r36 owner-log ragged-bottom correction implemented and source-verified; a replacement portable remains pending explicit owner instruction. The local-only report `C:\Users\admin\AppData\Local\GameTranslator\Diagnostics\Live\game-translator-live-live-stopped-20260830-060112-849-0003.txt` retained `10,569` lifecycle events with no dropped event or candidate failure. Geometry/text correlation confirmed exactly two simultaneous false `1 + 2` pre-OCR splits: adjacent gaps were `1` and `2 px`, shared vertical overlap was `0.989` and `1.0`, and the candidates persisted for `66` and `28` observations (`6,598.4` and `2,732.9 ms`). Every piece still produced one translation-input group, so post-OCR grouping was not the cause. Both triples passed width, top, center, shared-overlap and gap-discontinuity gates but exceeded only the ordinary normalized bottom-offset limit (`0.816` and `0.917` versus `0.75`). `CjkVertical` Auto now permits that stronger ragged-bottom shape only from the third column when shared overlap is at least `0.95`, normalized top offset is at most `0.05`, normalized bottom offset is at most `1.0`, and every inter-column gap is at most `2 px`; an otherwise matching `6 px` adjacent-bubble counterexample remains split. Explicit limits, non-`CjkVertical` profiles, OCR/stability, revision/source revalidation, cancellation/publication and provider/cache behavior are unchanged. Failing-first grouping tests reproduced only the two live splits (`29/31` before implementation), then grouping passed `31/31`, focused OCR/grouping/pipeline/profile tests passed `156/156`, focused calibration/candidate-region tests passed `29/29`, Release build completed with `0` warnings / `0` errors and the full Release suite passed `603/603`. The same report recorded `0` non-empty-to-empty-to-non-empty overlay transitions, no translated run of five repeated words and `3` sanitized outputs. No replacement portable, archive, visible UI launch, signing, staging, commit, push or publication was performed; the owner's running r36 process was left untouched.
- 2026-08-30 r37 unpacked portable candidate assembled and finalized for owner ragged-bottom live smoke (not archived, signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260830-ragged-bottom-cjk-grouping-r37`. It packages the r36 feature set plus the source-verified narrow `CjkVertical` ragged-bottom correction as a self-contained `win-x64` application with the pinned GPU Paddle runtime/model, normal Python bytecode cache and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The hidden packaged Tesseract smoke passed with `1` block / `10` characters in `352.4 ms`; packaged GPU Paddle verification passed with Ready `2,943.2 ms`, first result `4,183.0 ms` and `24` candidates. Packaged UI, Application, Infrastructure and Domain assemblies are byte-identical to their current applicable Release/RID outputs, and the packaged worker payload equals the current source after the release builder's established UTF-8 BOM. Independent SHA-256 verification passed all `30,728` manifest entries with no malformed, case-insensitive duplicate, unsafe, missing, mismatched or extra file. The app contains `30,722` files, including `10,426` ordinary Python bytecode-cache files, and is `5.014 GiB`; the complete candidate contains `30,729` files and is `5.019 GiB`. No r37 ZIP/archive was created, no packaged process remains, and no visible UI window was opened. The immediately preceding source verification remains Release build `0` warnings / `0` errors and full Release suite `603/603`; this is same-host packaging evidence pending owner live acceptance.
- 2026-08-30 owner-approved Chinese detector preset and BingWeb health hardening implemented and source-verified. The saved per-zone selector exposes `Standard` (`boxThreshold=0.60`), Chinese-only opt-in `ChineseExperimental` (`0.65`) and diagnostic `ChineseStrictExperimental` (`0.70`); missing profile values remain `Standard`, invalid enum values fail validation, and every non-Chinese request resolves back to `Standard`. A single persistent Paddle predictor receives threshold values per request without model reload. Lifecycle diagnostics record only requested/effective preset, numeric thresholds, raw count and confidence aggregates; existing bounded owner-authorized OCR/translation text diagnostics remain separate, and no raw provider response, credentials or frame pixels were added. BingWeb now bounds each request to `15 s`, reports the first consecutive timeout as warning, pauses for `60 s` after the second, pauses immediately on HTTP/provider `429` while honoring a longer valid `Retry-After`, sends no immediate retry, resets its counter after success and never changes providers automatically. An authoritative current Bing timeout/throttle retains the previous overlay without republishing a stale revision. Detector/profile/UI/pipeline focused tests passed `214/214`; Bing/provider/UI/pipeline focused tests passed `203/203`; the packaging verifier regression passed within `11/11` Paddle detector tests. Release build completed with `0` warnings / `0` errors and the full Release suite passed `621/621`. Docs mini-check found `0` Markdown-link and `0` actionable-backtick problems. No visible UI, signing, staging, commit, push or publication was performed; roadmap items 4-5 remain unchanged.
- 2026-08-30 local-only Chinese detector and recognizer research completed without production PP-OCRv5 wiring. One persistent packaged Paddle detector produced RCTW vertical candidates `8/8/7`, Chinese game horizontal `3/3/3` and MDPBench Chinese horizontal `62/61/57` at box thresholds `0.60`, `0.65`, `0.70`; `0.65` is therefore the only recommended owner-smoke opt-in and no automatic default promotion was made. The reproducible geometry-only report is `work/chinese-detector-ppocrv5-benchmark/detector-preset-ab-report.json`. On the same `11` detector crops, cached `PP-OCRv5_mobile_rec` and `PP-OCRv5_server_rec` each returned `10/11` non-empty results, with measured warm averages `14.4 ms` and `16.2 ms`; both handled horizontal Chinese game text better than production Tesseract but returned empty for a tall vertical crop where Tesseract returned text. The local mobile/server caches occupy about `17.2 MB` and `85.2 MB`. Scripts, crops, model caches and reports under `work/chinese-detector-ppocrv5-benchmark` are ignored/local-only, contain OCR text for owner research, are not eligible for upload and will not be packaged in r38. A production recognizer change remains gated on a broader annotated benchmark, vertical routing/packaging decisions and a separate ADR.
- 2026-08-30 r38 unpacked portable candidate assembled and finalized for owner Chinese/Bing live smoke (not archived, signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260830-chinese-detector-bing-hardening-r38`. It packages r37 plus the opt-in Chinese detector presets, privacy-safe detector diagnostics and BingWeb timeout/throttle visibility as a self-contained `win-x64` application with the pinned GPU Paddle detector runtime/model and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. The packaging verifier itself was updated failing-first to send the new complete Standard detector request. Hidden packaged Tesseract smoke passed with `1` block / `10` characters in `290.0 ms`; packaged Paddle verification passed with Ready `2,915.2 ms`, first result `4,225.6 ms`, `24` candidates and `threshold=0.30`, `boxThreshold=0.60`, `unclipRatio=1.20`. Independent rehashing passed all `30,728` manifest entries with zero malformed, case-insensitive duplicate, unsafe, missing, mismatched or extra files. Four packaged product assemblies match their current applicable RID outputs, and the packaged worker text matches source. The candidate contains `30,729` files and is `5.018 GiB`; it contains no PP-OCRv5 recognition model directory. No r38 transfer archive exists in the candidate root or release-candidates parent; the eight ZIP files under packaged CPython Lib/test are unchanged upstream test fixtures, not a generated portable archive. No packaged process remains and no visible UI window was opened. This is same-host packaging evidence pending owner live acceptance.
- 2026-08-30 owner-confirmed compact vertical-Chinese post-filter correction implemented and source-verified; replacement portable packaging follows under the same explicit owner command. The r38 reports showed two repeatedly detected and correctly grouped candidates (`3` members in `103x86`, `4` members in `112x96`) completing with zero recognized/translation blocks under every tested detector threshold and preprocessing preset. The production CJK target post-filter was rejecting each group solely because its aggregate was wider than tall. Failing-first tests reproduced both exact member geometries and a separated adjacent-bubble pair. The post-filter now retains its aggregate aspect rule and adds a narrow multi-member path only when at least two source rectangles are fully contained and every member independently meets the existing vertical ratio. The grouping algorithm and candidate bounds are unchanged; a single wide candidate and a group containing a horizontal member remain rejected. Focused candidate-region tests passed `16/16`, focused OCR/grouping/pipeline tests passed `111/111`, Release build completed with `0` warnings / `0` errors and the full Release suite passed `625/625`. OCR text stability, revision/source revalidation, cancellation/publication, provider/cache behavior, detector presets, all non-vertical-CJK semantics and roadmap items 4-5 are unchanged. Diagnostics add no new OCR, translation or provider-response text and retain the existing local-only owner policy.
- 2026-08-30 r39 unpacked portable candidate assembled and finalized for owner compact vertical-Chinese live smoke (not archived, signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260830-compact-cjk-postfilter-r39`. It packages r38 plus the source-verified narrow multi-column CJK post-filter correction with the same pinned GPU Paddle detector runtime and exactly `chi_sim`, `eng`, `jpn`, `jpn_vert`, `tha` Tesseract packs. Hidden packaged Tesseract smoke passed with `1` block / `10` characters in `330.5 ms`; packaged Paddle verification passed with Ready `3,281.3 ms`, first result `4,671.5 ms`, `24` candidates and the Standard detector request (`threshold=0.30`, `boxThreshold=0.60`, `unclipRatio=1.20`). Independent rehashing passed all `30,728` manifest entries with zero malformed, case-insensitive duplicate, unsafe, missing, mismatched or extra files. All four packaged product assemblies match their applicable current RID outputs, and the packaged worker text matches source. The app contains `30,722` files / `5.014 GiB`; the complete candidate contains `30,729` files / `5.018 GiB` and no PP-OCRv5 recognition model directory. No r39 archive exists in the candidate root or release-candidates parent, no packaged process remains and no visible UI window was opened. This is same-host packaging evidence pending owner live acceptance.
- 2026-08-30 r39 owner live smoke accepted the compact vertical-Chinese post-filter correction. Across eight Chinese sessions the report retained `41` completed wide multi-member candidates: `12` with two source columns, `19` with three and `10` with four; `33` produced non-empty OCR and one translated output, while `8` were OCR-empty. No accepted group exceeded four members in this run, and neither the lifecycle geometry nor owner observation showed an adjacent-bubble/page-wide merge. The exact four-column `115x101` owner case completed as one candidate with four OCR blocks and one translation input/output. A three-column `101x85` case also reached translation, although YandexWeb received only a partial Tesseract recognition, confirming a residual OCR-quality limitation rather than a post-filter rejection. Chinese translated outputs contained no run of three consecutive identical words, and no non-empty-to-empty-to-non-empty overlay gap shorter than one second occurred. BingWeb produced `28` translation-stage failures in three sessions: initial clusters aligned with the `15 s` request timeout and later immediate failures aligned with the active `60 s` pause; failures did not publish an empty snapshot over an existing overlay, and no HTTP `429` marker was present. YandexWeb Chinese/English sessions recorded no translation-stage failure. Pipeline restart and dropped-event counts remained zero and final failure state was clear. The stopped-session lifecycle does not retain enough failure-kind detail to prove which yellow/red UI state was visible after recovery; that is an observability limitation, not a grouping blocker. Local reports contain owner-authorized bounded OCR/translation text and must not be uploaded. No source, visible UI, archive, signing, staging, commit, push or publication was performed during this read-only review.
- 2026-08-30 clean-clone r39-class reproducibility follow-up completed from source commit `e21e156`. The checked-in Paddle runtime lock now pins the exact six-pack offline set `eng`, `jpn`, `jpn_vert`, `chi_sim`, `chi_sim_vert`, `tha`; build and finalization reject missing, unexpected or hash-mismatched packs, and the README/bootstrap examples request the same self-contained set. A same-host clean clone bootstrapped CPython `3.12.10`, PaddlePaddle GPU `3.2.1`, PaddleOCR `3.7.0`, PaddleX `3.7.2`, the pinned PP-OCRv6 model and all six traineddata files with GPU/model/hash verification passing. Its ignored/local-only unpruned self-contained candidate under `work/reproducibility/clean-clone-e21e156` passed hidden Tesseract smoke in `315.7 ms` and the normal packaged Paddle gate with `3,046.2 ms` to ready and `4,289.4 ms` to first result. Independent rehashing passed all `30,729` manifest entries with zero malformed, missing, extra or mismatched files; the candidate has `30,730` files and is `5.016 GiB`. All three detector-model files and all six actual r39 traineddata files match by SHA-256, and the packaged worker text matches after CRLF normalization. The non-PDB/non-log .NET app payload has the same `440`-file set: `435` files match by SHA-256, while `GameTranslator.UI.exe` and the four project DLLs retain equal sizes, versions and assembly identities but differ in build-path/debug provenance hashes. No production `src/**` file changed since the r39 code commit, so this proves offline functional reproduction, not path-independent byte-for-byte .NET output. No visible UI, archive, signing or artifact publication was performed.

- 2026-09-02 owner-approved transient detector-dropout correction implemented and source-verified after the wide-page colored-photo live review. A published candidate overlay is retained for at most the existing `2 s` readability interval only when an available detector briefly returns no regions and the resulting live snapshot would otherwise be empty. Reappearance without an empty publication requires the same candidate id and byte-exact crop source; capture loss, detector unavailability, source mismatch and interval expiry publish the current empty snapshot immediately. Failing-first coverage reproduced the one-frame flicker, then focused safety regressions passed `6/6`, all pipeline service tests passed `67/67`, and lifecycle/local-report tests passed `3/3`. Local lifecycle diagnostics now include bounded per-candidate confidence `0..1`; this adds no new OCR/translation/provider text or frame pixels, and diagnostic files remain local with no upload path. Detector/OCR thresholds, short-English filtering, candidate grouping/stability, revision/source revalidation, cancellation authority, provider/cache policy, all non-candidate semantics and roadmap items 4-5 are unchanged. Release build completed with `0` warnings / `0` errors and the full Release suite passed `629/629`. No visible UI, portable build, archive, signing, staging, commit, push or publication was performed.
- 2026-09-02 r40 unpacked portable candidate assembled and finalized for owner transient-dropout live smoke (not archived, signed, published or staged): `work/release-hardening/release-candidates/v0.1.0-pre.20260902-transient-detector-dropout-r40`. It packages the source-verified r40 correction as a self-contained `win-x64` application with the pinned GPU Paddle detector runtime/model and the complete locked Tesseract set `chi_sim`, `chi_sim_vert`, `eng`, `jpn`, `jpn_vert`, `tha`. Hidden packaged Tesseract smoke passed with `1` block / `10` characters in `289.6 ms`; packaged GPU Paddle verification passed with Ready `2,762.5 ms`, first result `3,799.7 ms`, `24` candidates and the Standard detector request (`threshold=0.30`, `boxThreshold=0.60`, `unclipRatio=1.20`). Independent rehashing passed all `30,729` manifest entries with zero malformed, case-insensitive duplicate, unsafe, missing, mismatched or extra files. All four packaged product assemblies are byte-identical to their applicable current Release outputs, and packaged worker text matches source after expected UTF-8 BOM/line-ending normalization. The app contains `30,723` files; the complete candidate contains `30,730` files / `5.020 GiB` and no PP-OCRv5 recognition model directory. The root ignored `chi_sim_vert` file was restored to its original pre-build SHA-256 after the packager temporarily consumed the previously verified lock-matching r39 copy. No r40 archive exists in the candidate root or release-candidates parent, no packaged process remains and no visible UI window was opened.
- 2026-09-02 BingWeb local-report observability implemented without changing provider behavior. `TranslatorProviderException` now retains the provider's absolute `NextRetryAt`; BingWeb uses the same cooldown boundary for the opening timeout/HTTP `429` failure and every subsequent fast-fail during that pause. Candidate `CandidateWorkFailed` lifecycle entries retain provider id, exact failure kind, available HTTP status, pause state, relative retry interval, absolute retry boundary and consecutive count. The report also summarizes the latest observed state as `Paused`, `RetryAllowed` or recovered after a confirmed network success. This closes the r39/r40 ambiguity where many candidate works completed near `15 s` but the report could not distinguish actual HTTP timeouts from work queued behind the existing three-slot translation limiter and then rejected by the open pause. Raw provider responses, credentials and frame pixels remain excluded and reports remain local-only. Timeout `15 s`, two-timeout threshold, `60 s` default pause, `Retry-After`, no-immediate-retry, no-fallback and overlay retention semantics are unchanged. Failing-first coverage was followed by focused provider/pipeline/live-diagnostics tests `83/83`, Release build with `0` warnings / `0` errors, full Release suite `629/629`, clean docs mini-check and clean `git diff --check` apart from line-ending notices. No visible UI, portable build, archive, staging, commit, push or publication was performed.
- 2026-09-03 Bing request-attempt and partial-replica follow-up implemented for owner smoke. Each Bing live cache miss now carries a local request id and bounded input from queue entry through provider invocation; credential GET and translation POST attempts record actual send/start/completion, outcome and available HTTP status. Paused fast-fail records no network send, and diagnostics from cancelled candidate work are drained without weakening revision/source publication authority. Separately, only `CjkVertical` live candidates receive an additional `300 ms` stability window after strict normalized prefix growth, reducing repeat translation of typewriter-style partial text without adding delay to initial/static or Horizontal/non-CJK input. No provider retry, batching, fallback, cache-key, detector/OCR/grouping boundary or roadmap item 4-5 changed. Raw responses, request forms/queries, credentials, tokens and pixels remain excluded; bounded local text reports must not be uploaded. Failing-first regressions and focused provider/pipeline/UI tests passed `167/167`; Release build completed with `0` warnings / `0` errors, the full Release suite passed `632/632`, and docs mini-check plus `git diff --check` were clean apart from line-ending notices.
- 2026-09-03 r41 unpacked owner-smoke portable assembled and finalized without an archive: `work/release-hardening/release-candidates/v0.1.0-pre.20260903-bing-attempt-cjk-typewriter-r41`. It is self-contained win-x64 and contains the locked PP-OCRv6 detector plus exactly `chi_sim`, `chi_sim_vert`, `eng`, `jpn`, `jpn_vert`, `tha`. Hidden packaged Tesseract smoke passed with `1` block / `10` characters in `286.6 ms`; packaged Paddle verification reached Ready in `2955.5 ms`, returned its first Standard 0.30/0.60/1.20 result in `4007.1 ms` and found `24` candidates. Independent verification rehashed all `30,729` manifest entries with zero malformed, duplicate, unsafe, missing, mismatched or extra paths. Product assemblies match the applicable Release outputs; model/traineddata hashes match the runtime lock; worker text matches source. Candidate size is `30,730` files / `5.020 GiB`; no r41 release archive or packaged process remains. The root owner file tessdata/chi_sim_vert.traineddata was restored after the temporary lock-matching packaging substitution and retains its original SHA-256 `ea672a78157199c333aa12ec4e74550077689b545df5fc770903716850c8b2e5`. No visible UI, signing, staging, commit, push or publication was performed.
- 2026-09-03 post-r41 English horizontal grouping correction implemented and source-verified after owner approval. The latest completed YandexWeb `en -> ru` report retained `203` completed candidates with exactly one translation-input group each, but geometry/text correlation found eight `2..6 px` continuation seams across seven long blocks immediately after a ten-line candidate; one coherent block was split `10 + 10 + 3`. A failing-first regression reproduced the exact `10 + 1` geometry. Only `SpacedLeftToRight` Auto now has no fixed line count: continuation after ten lines retains the existing strict `0.8` horizontal-overlap and `12 px` maximum-gap checks and additionally stops on a significant increase from the median line gap. Explicit `MaximumHorizontalLines` remains a hard override and all six non-spaced profiles retain the ten-line safety limit. Grouping tests passed `40/40`, focused OCR/grouping/architecture tests passed `148/148`, Release build completed with `0` warnings / `0` errors and the full Release suite passed `641/641`. Detector/OCR recognition, stability, revision/source revalidation, cancellation/publication, provider/cache behavior, CJK grouping and roadmap items 4-5 are unchanged. The diagnostic report is local-only and must not be uploaded. r41 does not contain this post-build correction; no replacement portable, archive, visible UI, signing, staging, commit, push or publication was performed.
- 2026-09-03 owner-authorized `D:` cleanup permanently removed the eight obsolete r34-r41 release-candidate directories (`46.349 GiB`), two reproducibility clean clones (`10.633 GiB`) and the obsolete `D:\GameTranslator-bootstrap-same-host-cleanroot-r1` (`5.054 GiB`). Exact-path verification found no process using them; `62.732 GiB` was returned. The repository, pinned `work/paddle-runtime-win-x64-r19`, AppData diagnostics, local OCR/research corpora and calibration/evidence were retained. These removed build outputs are not recoverable; their historical metadata remains in this document.
- 2026-09-03 r42 unpacked owner-smoke portable assembled and finalized without an archive: `work/release-hardening/release-candidates/v0.1.0-pre.20260903-adaptive-spaced-horizontal-r42`. It packages the post-r41 adaptive `SpacedLeftToRight` Auto correction as self-contained win-x64 with the locked PP-OCRv6 detector and exactly `chi_sim`, `chi_sim_vert`, `eng`, `jpn`, `jpn_vert`, `tha`. Hidden packaged Tesseract smoke passed with one block / ten characters in `296.2 ms`; packaged Paddle reached Ready in `2876.8 ms`, returned its first Standard result (`threshold=0.30`, `boxThreshold=0.60`, `unclipRatio=1.20`) in `3923.2 ms` and found `28` candidates. Independent verification rehashed all `30,729` manifest entries with zero malformed, duplicate, unsafe, missing, mismatched or extra paths. All four product assemblies match applicable Release outputs; the three model files and all six traineddata files match the runtime lock. The candidate contains `30,730` files / `5.020 GiB`, no root release archive and no running packaged process; its eight embedded ZIPs are ordinary CPython test fixtures. The temporary lock-matching packaging substitution was removed and the root owner `chi_sim_vert.traineddata` restored to SHA-256 `ea672a78157199c333aa12ec4e74550077689b545df5fc770903716850c8b2e5`. No visible UI, signing, staging, commit, push or publication was performed.
- 2026-09-04 r43 unpacked owner-smoke portable assembled and finalized without an archive: `work/release-hardening/release-candidates/v0.1.0-pre.20260904-welcome-tour-parameter-help-r43`. It packages r42 plus compact hover help for every manually editable parameter group and the seven-step Russian first-run welcome tour with Back/Next/Done navigation, a top-right close button, local versioned completion state and manual restart from `Hotkeys & Settings`. The tour does not mutate profile settings, start capture or contact a provider. Hidden packaged Tesseract smoke passed with one block / ten characters in `292.3 ms`; packaged Paddle reached Ready in `2749.8 ms`, returned its first Standard result (`threshold=0.30`, `boxThreshold=0.60`, `unclipRatio=1.20`) in `3780.2 ms` and found `28` candidates. Independent verification rehashed all `30,729` manifest entries with zero malformed, duplicate, unsafe, missing, mismatched or extra paths. All four product assemblies match applicable Release outputs; the three model files and exactly `chi_sim`, `chi_sim_vert`, `eng`, `jpn`, `jpn_vert`, `tha` match the runtime lock. The candidate contains `30,730` files / `5.020 GiB`, no root release archive and no running packaged process. The temporary lock-matching packaging substitution was removed and the root owner `chi_sim_vert.traineddata` restored to SHA-256 `ea672a78157199c333aa12ec4e74550077689b545df5fc770903716850c8b2e5`. No visible UI, signing, staging, commit, push or publication was performed.
- 2026-09-04 post-r43 UI correction: owner smoke showed that r43's welcome card obscured the workspace without identifying its referenced controls, and that a parameter-help popup could remain open when the pointer moved from `i` onto the popup. Current source assigns each of the seven steps a concrete visible target, uses an `Exclude` geometry plus accent border for a true spotlight, scrolls the target into view and chooses the least-overlapping card corner. The help popup now observes only its sibling icon circle's hover state. Focused tests passed `4/4`, including all seven spotlights at `1024x640`, `1280x720`, `1600x900`, `1920x1080` and `2560x1440`; Release build completed with zero warnings/errors and the full suite passed `645/645`. r43 does not contain this post-package correction; no replacement portable has been assembled yet.
- 2026-09-04 direct-Release startup correction: the owner's first file and `dotnet run --no-build` attempts exited immediately after `Main window resolved from dependency injection`. Windows `.NET Runtime` event `1026` identified the exact unhandled `XamlParseException`: the welcome overlay's early `IsVisibleChanged` reset resolved `WelcomeTourDimmingPath` before the `DataTemplate` had created that child. A failing-first test now invokes reset before template materialization, and the implementation safely clears only cached children during that phase. The corrected Release output rebuilt with zero warnings/errors; focused UI/startup tests passed `4/4` and the full suite passed `645/645`. No visible post-fix launch, replacement portable, signing, staging, commit, push or publication was performed.
- 2026-09-04 documentation/default review: project and module documentation was reconciled with current `main`; four module developer READMEs and one cohesive Russian user guide now cover installation, first run, OCR packs, providers, profiles, overlay, cache, hotkeys and diagnostics. Safe defaults were verified against source: no provider/language/zone is silently selected, `Standard` detector and `DialogComic` bounded policy remain default, live timing is `Balanced`, and the welcome tour performs no capture or provider call. This completes the documentation scope of Issue `#29` without creating or publishing a replacement portable.
- 2026-09-04 r44 unpacked source-equivalent portable assembled and finalized without an archive: `work/release-hardening/release-candidates/v0.1.0-pre.20260904-post-r43-current-source-r44`. It packages published HEAD `20e2e1b3611002d8a2974926185fe0f31bd277bf`, including the post-r43 spotlight, startup-safety, persistent tour action and OCR-guidance corrections, as self-contained win-x64 with the pinned GPU Paddle detector runtime/model and exactly `chi_sim`, `chi_sim_vert`, `eng`, `jpn`, `jpn_vert`, and `tha`. Hidden packaged Tesseract smoke passed with one block / ten characters in `325.6 ms`; packaged Paddle reached Ready in `2731.3 ms`, returned its first Standard result in `3892.1 ms`, and found `28` candidates. Independent rehashing passed all `30,729` checksum entries with zero malformed, duplicate, unsafe, missing, mismatched, or extra paths. Applicable product assemblies match the current Release outputs, the packaged worker text matches source, and all six language packs match the runtime lock. The candidate contains `30,730` files / `5.020 GiB`; no transfer archive exists in its root or the release-candidates parent, no packaged process remains, and no visible UI was opened. The root owner `chi_sim_vert.traineddata` was restored to SHA-256 `ea672a78157199c333aa12ec4e74550077689b545df5fc770903716850c8b2e5`. This is local same-host package evidence, not signing, owner-live acceptance, or publication.
- 2026-09-04 owner-authorized `D:` cleanup after r44 verification permanently removed the superseded r42 and r43 candidate directories plus the temporary r44 build-runner/log/backup directory. The removed files occupied `10,794,077,994` bytes; observed free space on `D:` increased by `10,934,476,800` bytes to `37,530,091,520` bytes. Exact targets were constrained to the release-candidates and temporary work directories; r44, the pinned runtime r19, source Release outputs, research/evidence data, AppData diagnostics, and all nine protected tracked paths were retained. The removed local build outputs are not recoverable; their historical records remain above.
- 2026-09-04 owner-authorized `C:` cleanup permanently removed the obsolete August 1 Paddle runtime plus the GPU Paddle and EasyOCR benchmark runtime/model directories from the old Codex workspace: `77,719` files / `15,255,581,063` bytes. Before deletion, `33` benchmark JSON/input/measurement files, evidence PNGs, reproduction scripts, and the old `pyvenv.cfg` (`15,889,978` bytes) were copied with their original relative structure to ignored local evidence at `work/historical-benchmark-evidence/2026-08-01-c-runtime-cleanup`; all `33` SHA-256 records were rechecked with zero malformed, missing, mismatched, or extra paths. Observed free space on `C:` increased by `15,435,202,560` bytes to `92,071,809,024` bytes. The external OCR corpus and its package-verification sample, Game Translator AppData/profiles/diagnostics, the registered 1.0.24 installation, and unrelated WeMod `SquirrelTemp` remained untouched. The removed environments are not recoverable and must be recreated if historical benchmarks are rerun.

Consolidated unpublished-worktree inventory: `docs/agents/local-unpublished-worktree-status.md`.

## Next Delivery Guidance

- Track D Issue `#41` retains the evidence trail; Change Request `#47` and ADR-030 record the owner-authorized default transition. ADR-025's `<= 5.0 s` ready-session translated-overlay SLO remains binding. Python/Paddle import and detector-load optimization remains a separate performance task, not a reason to restore legacy full-page selection.
- Issue `#29` documentation/default scope is complete. Issue `#30` remains open for a source-equivalent package, release notes, applicable QG21 evidence and explicit owner publication approval.
- Writing-system parent `#48` remains open while `#49` and `#53`-`#55` are incomplete. CJK horizontal/vertical (`#50`/`#51`) and Thai (`#52`) are complete; do not reopen them to absorb unrelated language cohorts.
- Issues `#34` and `#35` remain human/owner review tasks. `#35` owns the explicit disposition of the nine protected calibration paths; documentation maintenance must not stage, revert or regenerate them.
- ADR-030 authorizes the packaged detector adapter as the target default path under the accepted evidence. Any further OCR runtime, non-Tesseract recognizer, provider-default change, game-process feature or retry/fallback policy still requires its own owner decision.

## Parallelization Guidance

Independent work may proceed on `#34`, `#35`, `#49`, and the separate `#53`-`#55` cohorts when each task's own evidence and dependencies are satisfied. Release `#30` remains owner-gated. Do not combine specialized writing-system rules, calibration promotion, provider-default changes, OCR contract changes, or release publication into one implicit scope.

## Status Update Rules

When any item starts or completes:
- Update its `Status` line in this file.
- Add issue/PR references if created.
- Add verification results and evidence paths with tracked/generated/local/build-output labels when applicable.
- Keep this file concise; move detailed evidence to issues or dedicated reports.
